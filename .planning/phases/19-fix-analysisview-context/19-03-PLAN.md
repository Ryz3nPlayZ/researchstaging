---
phase: 19-fix-analysisview-context
plan: 03
type: execute
wave: 2
depends_on: [19-02]
files_modified: [backend/analysis_api.py]
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "generate-code endpoint accepts UUID strings as project_id"
    - "Both generate-code and execute endpoints use consistent project_id type (str)"
    - "Type annotation matches database schema (Project.id is String(36))"
  artifacts:
    - path: "backend/analysis_api.py"
      provides: "Data analysis API endpoints with correct type annotations"
      contains: "project_id: str"
  key_links:
    - from: "backend/analysis_api.py:69"
      to: "backend/database/models.py:96"
      via: "Type consistency (str)"
      pattern: "project_id: str"
---

<objective>
Fix Type Annotation in generate-code Endpoint

This plan closes a P2 (Low) priority gap. The generate-code endpoint has incorrect type annotation for project_id parameter. Line 69 declares `project_id: int` but should be `project_id: str` to match the database schema where Project.id is String(36) (UUID).

Purpose: Ensure API type annotations match database schema
Output: Corrected type annotation for consistency

Note: The execute endpoint (line 124) already has the correct `project_id: str` type. This change brings the generate-code endpoint in line with the execute endpoint.
</objective>

<execution_context>
@/home/zemul/.claude/get-shit-done/workflows/execute-plan.md
@/home/zemul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-fix-analysisview-context/19-fix-analysisview-context-UAT.md
@backend/database/models.py
@backend/analysis_api.py
</context>

<tasks>

<task type="auto">
  <name>Change project_id type from int to str in generate-code endpoint</name>
  <files>backend/analysis_api.py</files>
  <action>
    1. Read backend/analysis_api.py
    2. Find line 69 in the generate_code function definition
    3. Change: `project_id: int,` to `project_id: str,`
    4. Verify the change matches the execute endpoint at line 124

    The change is minimal but important for type consistency:
    - Before: `async def generate_code(project_id: int, request: CodeGenerationRequest, ...)`
    - After: `async def generate_code(project_id: str, request: CodeGenerationRequest, ...)`

    This aligns with the database schema where Project.id is defined as:
    `id = Column(String(36), primary_key=True, default=lambda: str(uuid.uuid4()))`
  </action>
  <verify>
    # Verify the type is now str
    grep -n "async def generate_code" backend/analysis_api.py
    # Should show project_id: str on the next line
    sed -n '69p' backend/analysis_api.py
  </verify>
  <done>
    - Line 69 shows `project_id: str` (not int)
    - Both generate-code and execute endpoints use same type
    - Type matches database models.py Project.id definition
  </done>
</task>

</tasks>

<verification>
1. Confirm type annotation changed:
   ```bash
   sed -n '69p' backend/analysis_api.py
   ```
   Expected: Contains `project_id: str,`

2. Confirm consistency with execute endpoint:
   ```bash
   grep -n "project_id:" backend/analysis_api.py
   ```
   Expected: Both line 69 and line 124 show `project_id: str`

3. Verify database schema reference:
   ```bash
   grep -A2 "class Project" backend/database/models.py | grep "id = Column"
   ```
   Expected: Shows `String(36)` confirming UUID type

4. Test endpoint accepts UUID (after server restart from plan 19-02):
   ```bash
   TEST_UUID=$(uuidgen)
   curl -X POST "http://localhost:8000/api/analysis/projects/$TEST_UUID/generate-code" \
     -H "Content-Type: application/json" \
     -d '{"task":"test","language":"python"}' -w "\nHTTP Status: %{http_code}\n"
   ```
   Expected: 400 (missing task) or 500 (LLM error), NOT 422 (validation error for UUID format)
</verification>

<success_criteria>
1. Line 69 of analysis_api.py has `project_id: str` (not int)
2. Type annotation matches execute endpoint (line 124)
3. Type annotation matches database schema (Project.id is String(36))
4. Generate-code endpoint accepts UUID format without 422 validation error
</success_criteria>

<output>
After completion, create `.planning/phases/19-fix-analysisview-context/19-03-SUMMARY.md`
</output>
