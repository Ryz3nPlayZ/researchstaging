---
phase: 07-data-analysis
plan: 03
type: execute
wave: 3
depends_on: ["07-01", "07-02"]
files_modified:
  - backend/analysis_api.py
  - frontend/src/components/analysis/AnalysisResults.jsx
  - frontend/src/components/analysis/CodeEditor.jsx
  - frontend/src/lib/api.js
autonomous: true
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "User can view analysis results as formatted tables"
    - "User can view analysis results as charts (line, bar, scatter plots)"
    - "User can view analysis results as visualizations (heatmaps, histograms)"
    - "User can download analysis results as CSV file"
    - "User can download analysis results as PNG image (for plots)"
    - "Code editor integrates with execution and results display"
  artifacts:
    - path: "frontend/src/components/analysis/AnalysisResults.jsx"
      provides: "Results display component with tables, charts, downloads"
      min_lines: 200
    - path: "backend/analysis_api.py"
      provides: "Result download and visualization endpoints"
      exports: ["/api/analysis/results/{id}/download", "/api/analysis/results/{id}/visualize"]
      min_lines: 60 (additional lines)
    - path: "frontend/src/components/analysis/CodeEditor.jsx"
      provides: "Integrated code editing, execution, and results"
      contains: "onExecute, showResults"
  key_links:
    - from: "frontend/src/components/analysis/CodeEditor.jsx"
      to: "/api/analysis/execute"
      via: "POST request to execute code"
      pattern: "fetch.*api/analysis/execute"
    - from: "frontend/src/components/analysis/AnalysisResults.jsx"
      to: "/api/analysis/results/{id}/download"
      via: "GET request to download results"
      pattern: "fetch.*download"
    - from: "frontend/src/components/analysis/CodeEditor.jsx"
      to: "frontend/src/components/analysis/AnalysisResults.jsx"
      via: "Component import and rendering"
      pattern: "import.*AnalysisResults"
---

<objective>
Implement analysis results display with tables, charts, visualizations, and download functionality.

Purpose: Provide rich visualization of analysis execution results including tabular data, interactive charts, and export options for CSV and image downloads.

Output: AnalysisResults component with tables/charts/visualizations, download API endpoints, and integrated CodeEditor workflow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-data-analysis/07-01-PLAN.md
@.planning/phases/07-data-analysis/07-02-PLAN.md

# Existing infrastructure:
# - backend/execution_service.py: Code execution with output capture (from Plan 07-02)
# - backend/memory_service.py: Findings stored with analysis results
# - frontend/src/components/analysis/CodeEditor.jsx: Code editor from Plan 07-01
# - frontend/src/components/ai/AISidebar.jsx: Chat interface with code generation

# What needs to be built:
# 1. AnalysisResults component for displaying tables, charts, visualizations
# 2. Download API endpoints for CSV and PNG export
# 3. Integration with CodeEditor for execute → results → download workflow
# 4. Chart rendering with Plotly or similar library
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalysisResults component with tables and charts</name>
  <files>frontend/src/components/analysis/AnalysisResults.jsx, frontend/package.json</files>
  <action>
Install dependencies for data visualization:
npm install plotly.js react-plotly.js recharts papaparse

Create frontend/src/components/analysis/AnalysisResults.jsx:

1. Component props:
   - result: {success, output, error, execution_time, finding_id}
   - onClose: function (callback to close results panel)

2. Parse output intelligently:
   - Detect CSV format (comma/tab-separated values)
   - Detect JSON format (parseable JSON)
   - Detect matplotlib/plotly code hints (generate placeholder chart)
   - Fallback to plain text display

3. Display modes:
   a) Table view:
      - Use Recharts Table or simple HTML table
      - Parse CSV/JSON data into table structure
      - Sortable columns, pagination for large datasets
      - Export to CSV button

   b) Chart view:
      - Use react-plotly.js for interactive charts
      - Auto-detect chart type from output patterns:
        * "plt.plot()" or "plot(" → Line chart
        * "plt.bar()" or "barplot(" → Bar chart
        * "plt.scatter()" or "plot(..., type='p')" → Scatter plot
        * "plt.hist()" or "hist(" → Histogram
      - Provide chart type selector if auto-detect fails
      - Support: line, bar, scatter, pie, heatmap

   c) Visualization view:
      - Heatmap for correlation matrices
      - Box plots for statistical distributions
      - Violin plots for data density

   d) Text view (fallback):
      - Pre-formatted code block with syntax highlighting
      - Copy to clipboard button

4. Download buttons:
   - Download as CSV (for tabular data)
   - Download as PNG (for charts using Plotly export)
   - Download as TXT (for raw output)

5. Execution info:
   - Display execution time
   - Display success/error status
   - Show finding_id with link to memory

Use Shadcn Tabs, Table, Button components.
Follow existing component patterns from AIAssistant.jsx.
</action>
  <verify>
grep -n "AnalysisResults" frontend/src/components/analysis/AnalysisResults.jsx
grep -n "Plot\|Chart\|Table" frontend/src/components/analysis/AnalysisResults.jsx
</verify>
  <done>
AnalysisResults component renders tables, charts, and text output
CSV download functionality works
PNG export works for charts
</done>
</task>

<task type="auto">
  <name>Task 2: Create download API endpoints for analysis results</name>
  <files>backend/analysis_api.py</files>
  <action>
Update backend/analysis_api.py with download endpoints:

1. GET /api/analysis/results/{finding_id}/download
   - Query param: format (csv, txt, json)
   - Retrieve Finding from memory by finding_id
   - Parse finding_data based on format:
     * CSV: Convert output to CSV format
     * TXT: Return raw output as text
     * JSON: Return finding_data as JSON
   - Set appropriate Content-Type header:
     * CSV: text/csv
     * TXT: text/plain
     * JSON: application/json
   - Set Content-Disposition header for download
   - Return file content directly

2. GET /api/analysis/results/{finding_id}/visualize
   - Retrieve Finding from memory
   - Detect visualization type from output
   - Return: {type: "table" | "chart" | "text", data: object, chart_config: object}
   - Include recommended chart type and configuration for frontend

3. Error handling:
   - Return 404 if finding_id not found
   - Return 400 for invalid format parameter
   - Return 500 for data parsing failures

Follow existing API patterns from memory_api.py (findings endpoints).
</action>
  <verify>
grep -n "/download" backend/analysis_api.py
grep -n "/visualize" backend/analysis_api.py
grep -n "Content-Type" backend/analysis_api.py
</verify>
  <done>
Download endpoint returns results in CSV, TXT, or JSON format
Visualize endpoint returns structured data for frontend rendering
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate CodeEditor with execution and results display</name>
  <files>frontend/src/components/analysis/CodeEditor.jsx, frontend/src/lib/api.js</files>
  <action>
Update frontend/src/lib/api.js:

1. Add executeAnalysisCode() method:
   - POST /api/analysis/projects/{projectId}/execute
   - Accepts: {code, language, saveToMemory}
   - Returns: {success, output, error, executionTime, findingId}
   - Error handling for execution failures

2. Add downloadAnalysisResult() method:
   - GET /api/analysis/results/{findingId}/download?format={format}
   - Returns blob/file for download
   - Triggers browser download

Update frontend/src/components/analysis/CodeEditor.jsx:

1. Add onExecute prop handler:
   - When Run button clicked, call executeAnalysisCode()
   - Show loading spinner during execution
   - On success, open AnalysisResults dialog/modal with results
   - On error, show toast notification with error message

2. Integrate AnalysisResults component:
   - Import AnalysisResults component
   - Render in Dialog/Modal when execution completes
   - Pass result data to AnalysisResults
   - Close dialog when user dismisses results

3. UI improvements:
   - Add language indicator badge (Python/R)
   - Add execution status indicator (idle, running, completed, error)
   - Add execution time display after completion
   - Keyboard shortcut: Ctrl+Enter or Cmd+Enter to execute

Follow existing patterns from AISidebar message handling.
Use Shadcn Dialog component for results modal.
</action>
  <verify>
grep -n "executeAnalysisCode" frontend/src/lib/api.js
grep -n "onExecute\|AnalysisResults" frontend/src/components/analysis/CodeEditor.jsx
</verify>
  <done>
CodeEditor Run button executes code and displays results
AnalysisResults dialog shows tables, charts, and download options
Execution status and time displayed in UI
</done>
</task>

</tasks>

<verification>
- [ ] npm install plotly.js react-plotly.js recharts papaparse succeeds
- [ ] AnalysisResults component displays tabular data in table format
- [ ] AnalysisResults component displays charts (line, bar, scatter)
- [ ] CSV download button works and exports data correctly
- [ ] PNG export works for charts
- [ ] CodeEditor Run button executes code and shows results
- [ ] Results display shows execution time and success/error status
</verification>

<success_criteria>
- All tasks completed
- Tables render correctly from CSV/JSON output
- Charts auto-detect type and render interactively
- Download functionality works for CSV, TXT, PNG formats
- Code editor → execution → results workflow works end-to-end
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-analysis/07-03-SUMMARY.md`
</output>
