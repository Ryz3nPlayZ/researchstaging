---
phase: 19-fix-analysisview-context
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
gap_closure: true

must_haves:
  truths:
    - "Backend server is restarted with latest code"
    - "FastAPI endpoints use current codebase, not stale version"
    - "Server responds to health check with loaded modules"
  artifacts: []
  key_links: []
---

<objective>
Restart Backend Server to Load Latest Code

This plan closes a P0 (Blocker) operational gap identified during UAT. The backend server (PID 16453) was started at 22:49, BEFORE recent commits were made. The server is running stale code and needs to be restarted to load the latest changes.

Purpose: Ensure backend endpoints execute current code, not outdated version
Output: Running backend server with fresh code loaded

Critical context: The execute endpoint (analysis_api.py line 124) already has the correct `project_id: str` type. The 404 errors during testing were caused by the server running old code, not by incorrect code in the current repository.
</objective>

<execution_context>
@/home/zemul/.claude/get-shit-done/workflows/execute-plan.md
@/home/zemul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-fix-analysisview-context/19-fix-analysisview-context-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Restart backend server with latest code</name>
  <files></files>
  <action>
    1. Verify the stale server process: `ps aux | grep "python server.py" | grep -v grep`
    2. Kill the old server process: `kill 16453` (or use `pkill -f "python server.py"`)
    3. Verify process is killed: `ps aux | grep "python server.py" | grep -v grep`
    4. Start fresh server: `cd backend && python server.py`
    5. Verify server started: `curl -s http://localhost:8000/api/ | head -5`

    Note: This is an operational task, not a code change. The code in the repository is already correct - the server just needs to reload.
  </action>
  <verify>
    # Verify server is running and responding
    curl -s http://localhost:8000/api/ | head -5
    # Check for new PID (not 16453)
    ps aux | grep "python server.py" | grep -v grep
  </verify>
  <done>
    - Old server process (PID 16453) is terminated
    - New server process is running with different PID
    - Health check endpoint returns 200
    - Server log shows "Application startup complete" with current timestamp
  </done>
</task>

</tasks>

<verification>
1. Confirm old process is gone:
   ```bash
   ps aux | grep 16453 | grep -v grep
   ```
   Expected: No output (process terminated)

2. Confirm new server is running:
   ```bash
   curl -s http://localhost:8000/api/analysis/health
   ```
   Expected: `{"status":"healthy","service":"analysis"}`

3. Verify server startup time is recent (within last 5 minutes):
   ```bash
   ps aux | grep "python server.py" | grep -v grep | awk '{print $9}'
   ```
   Expected: Time stamp showing recent start (not 22:49)

4. Test execute endpoint with UUID:
   ```bash
   curl -X POST http://localhost:8000/api/analysis/projects/$(uuidgen)/execute \
     -H "Content-Type: application/json" \
     -d '{"code":"print(1+1)","language":"python","save_to_memory":false}'
   ```
   Expected: Either 200 (success) or 404 (project not found), NOT 422 (validation error for UUID format)
</verification>

<success_criteria>
1. Old backend process (PID 16453) is terminated
2. New backend process is running with current PID
3. Server health check returns 200 OK
4. Execute endpoint accepts UUID format without 422 validation error
5. Server startup time is recent (not from 22:49)
</success_criteria>

<output>
After completion, create `.planning/phases/19-fix-analysisview-context/19-02-SUMMARY.md`
</output>
