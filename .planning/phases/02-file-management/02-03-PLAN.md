# Plan 02-03: Navigator Integration and Routing

**Phase:** 2 - File & Project Management
**Plan:** 3 of 4
**Type:** Integration
**Wave:** 1 (Can start after 02-02)
**Depends on:** 02-02 (needs FileExplorer component)
**Files modified:** `frontend/src/components/layout/Navigator.jsx`, `frontend/src/App.js`
**Autonomous:** Yes (integration work)
**Must haves:**
1. FileExplorer integrated into Navigator panel
2. Navigator state management (files vs tasks view toggle)
3. File selection updates Inspector panel
4. Breadcrumb navigation for folder hierarchy
5. View mode switcher (tree view vs list view)

## Overview

Integrate the FileExplorer component into the Navigator panel with proper state management, view switching, and interaction with other panels (Inspector, Workspace). The Navigator becomes the primary location for file browsing while maintaining task visibility.

## Tasks

### Task 1: Update Navigator component to include FileExplorer

**File:** `frontend/src/components/layout/Navigator.jsx`

**Changes:**
- Add `FileExplorer` component to Navigator
- Create view state: 'tasks' vs 'files' (tabs or toggle)
- Show task list in 'tasks' view (existing functionality)
- Show FileExplorer in 'files' view (new functionality)
- Pass selected project to FileExplorer
- Update Navigator header to show current view title

**Success criteria:**
- Navigator displays tab switcher (Tasks | Files)
- Tasks view shows existing task list
- Files view shows FileExplorer component
- Switching views preserves state
- Project context passed correctly

### Task 2: Implement file selection and Inspector updates

**Files:** `frontend/src/components/files/FileExplorer.jsx`, `frontend/src/components/layout/Inspector.jsx`

**Changes:**
- Add file selection state to FileExplorer
- On file click: emit event or update context with selected file
- Update Inspector to show file details when file selected:
  - File name, type, size
  - Created/modified dates
  - Metadata (PDF page count, CSV rows, etc.)
  - Preview (if available: text preview, PDF first page)
- Clear selection when clicking empty space

**Success criteria:**
- Clicking file selects it (visual highlight)
- Inspector shows file details
- Inspector shows preview for text-based files
- Selection cleared when clicking empty space
- Inspector shows previous state when no file selected

### Task 3: Add breadcrumb navigation for folder hierarchy

**File:** `frontend/src/components/files/FileExplorer.jsx`

**Changes:**
- Add breadcrumb bar at top of file explorer
- Show path: `Project Files > Literature > Papers`
- Click breadcrumb segment to navigate to that folder
- Update breadcrumbs as user clicks into folders
- Add "Up" button to navigate to parent folder

**Success criteria:**
- Breadcrumbs show current folder path
- Clicking breadcrumb navigates to that folder
- "Up" button goes to parent folder
- Breadcrumbs update when folder selected in tree
- Root level shows "Project Files" only

### Task 4: Implement view mode switcher (tree vs list)

**File:** `frontend/src/components/files/FileExplorer.jsx`

**Changes:**
- Add view toggle: tree view (default) vs list view (flat)
- Tree view: hierarchical folder structure (existing)
- List view: flat list of all files with sortable columns (name, type, size, date)
- Preserve view preference in localStorage
- Update view button icon based on current mode

**Success criteria:**
- Toggle button switches between tree and list views
- Tree view shows nested folders
- List view shows flat file list with sort headers
- Click column headers to sort (name, size, date ascending/descending)
- View preference saved across sessions

### Task 5: Add file quick actions from Navigator

**File:** `frontend/src/components/files/FileExplorer.jsx`

**Changes:**
- Add quick action buttons on hover: download, delete, info
- Show file type icon with appropriate color
- Add "Copy path" button on file hover
- Add "Open in editor" button for text-based files (.md, .py, .r, .js)
- Ensure actions don't interfere with drag-drop

**Success criteria:**
- Hover actions appear on file hover
- Download triggers file download
- Delete shows confirmation
- Copy path copies to clipboard
- Open in editor opens file in Workspace (if supported)

## Definition of Done

- [ ] FileExplorer integrated into Navigator panel
- [ ] View switcher (Tasks | Files) functional
- [ ] File selection updates Inspector panel
- [ ] Breadcrumb navigation working
- [ ] Tree and list view modes working
- [ ] File quick actions functional
- [ ] Keyboard navigation (arrow keys, enter to open)
- [ ] Responsive design (collapsible on mobile)

## Testing Strategy

1. Switch between Tasks and Files view → verify state preserved
2. Click file in FileExplorer → verify Inspector shows file details
3. Click breadcrumb segments → verify navigates to folders
4. Switch to list view → verify flat list renders
5. Sort list by different columns → verify sorting works
6. Hover file → verify quick actions appear
7. Click "Open in editor" for text file → verify opens in Workspace
8. Test keyboard navigation (arrow keys, enter)
