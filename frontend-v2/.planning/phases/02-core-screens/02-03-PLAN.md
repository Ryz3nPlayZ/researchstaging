---
phase: 02-core-screens
plan: 03
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend-v2/src/components/tasks/TaskStatusBadge.tsx
  - frontend-v2/src/components/tasks/TaskCard.tsx
  - frontend-v2/src/components/tasks/TaskDetail.tsx
  - frontend-v2/src/pages/ProjectWorkspace.tsx
autonomous: false

must_haves:
  truths:
    - "User can see active project name and research goal"
    - "User can see task list with status badges"
    - "User can click task to see details in DetailsPanel"
    - "User can navigate between different views"
    - "Sidebar navigation highlights active view"
  artifacts:
    - path: "frontend-v2/src/components/tasks/TaskStatusBadge.tsx"
      provides: "Status badge component for tasks"
      min_lines: 30
    - path: "frontend-v2/src/components/tasks/TaskCard.tsx"
      provides: "Task card component for task list"
      min_lines: 50
    - path: "frontend-v2/src/components/tasks/TaskDetail.tsx"
      provides: "Task detail view for sidebar"
      min_lines: 60
    - path: "frontend-v2/src/pages/ProjectWorkspace.tsx"
      provides: "Project workspace page with navigation and task list"
      min_lines: 100
  key_links:
    - from: "ProjectWorkspace.tsx"
      to: "projects.ts"
      via: "getProject API call on mount to fetch project data"
    - from: "ProjectWorkspace.tsx"
      to: "useUIStore"
      via: "setActiveView for navigation highlighting"
    - from: "TaskCard.tsx"
      to: "TaskDetail.tsx"
      via: "onClick opens DetailsPanel with task details"
---

<objective>
Build Project Workspace page - main project view with sidebar navigation, task list, and task details panel.

Purpose: Create the primary workspace where users manage their research projects, view tasks, and monitor progress. This is where users spend most of their time.

Output: Project Workspace page with navigation, task list, and task details integration.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@frontend-v2/.planning/PROJECT.md
@frontend-v2/.planning/ROADMAP.md
@frontend-v2/.planning/STATE.md
@frontend-v2/.planning/phases/01-frontend-foundation/01-03-SUMMARY.md
@.planning/UI_UX_SPECIFICATION.md
@frontend-v2/src/types/project.ts
@frontend-v2/src/types/task.ts
@frontend-v2/src/stores/useProjectStore.ts
@frontend-v2/src/stores/useUIStore.ts
@frontend-v2/src/components/layout/WorkspaceLayout.tsx
@frontend-v2/src/components/layout/Sidebar.tsx
@frontend-v2/src/components/layout/DetailsPanel.tsx
@frontend-v2/src/services/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TaskStatusBadge component</name>
  <files>frontend-v2/src/components/tasks/TaskStatusBadge.tsx</files>
  <action>
    Create frontend-v2/src/components/tasks/TaskStatusBadge.tsx:

    **Props:**
    - status: TaskState (pending, running, completed, failed, blocked)
    - size?: 'sm' | 'md' (default: 'md')

    **Display:**
    Map TaskState to visual badge:
    - **pending:** Gray background, "Pending" text
    - **running:** Blue background, "Running" text
    - **completed:** Green background, "Completed" text
    - **failed:** Red background, "Failed" text
    - **blocked:** Orange/Yellow background, "Blocked" text

    **Styling:**
    - Rounded (pill shape)
    - Padding based on size
    - Use design token functional colors
    - Small font size
    - Inline-flex for centering
    - Uppercase or title case text

    **Sizes:**
    - sm: Smaller padding and text (for tight lists)
    - md: Standard padding and text (default)

    **Implementation:**
    - Use switch statement or object mapping for status → styles
    - Return null or handle unknown status gracefully

    This component will be used in TaskCard and other places to display task status.
  </action>
  <verify>TaskStatusBadge displays correct colors for each status</verify>
  <done>Task status badge component with color coding</done>
</task>

<task type="auto">
  <name>Task 2: Create TaskCard component</name>
  <files>frontend-v2/src/components/tasks/TaskCard.tsx</files>
  <action>
    Create frontend-v2/src/components/tasks/TaskCard.tsx:

    **Props:**
    - task: Task (from types/task.ts)
    - onClick: () => void (for opening details)
    - isActive?: boolean (if this is the selected task)

    **Display:**
    1. **Card Structure:**
       - Clickable card (div or button with button-like styling)
       - Border or shadow
       - Hover effect (elevation)
       - Active state (highlight if selected)

    2. **Content:**
       - Task name (bold, top)
       - Task type badge (e.g., "search", "summary")
       - TaskStatusBadge (top right or below name)
       - Brief description or metadata (optional, truncate if long)
       - Created date (small, muted)

    3. **Visual Indicators:**
       - Left border color by status (optional)
       - Icon or dot indicator for running tasks (optional pulse animation)

    **Styling:**
    - Use design tokens
    - Card layout with proper spacing
    - Hover: slight elevation (shadow)
    - Active: darker border or background
    - Cursor pointer
    - Transition effects

    **Behavior:**
    - onClick: Triggers callback to open task details
    - Entire card is clickable
    - Keyboard accessible (Enter key to activate)

    **Usage:**
    - Will be used in task list in ProjectWorkspace
    - Also useful in other places that show tasks

    This component represents a task in a list and provides a way to view details.
  </action>
  <verify>TaskCard displays task info with status badge and hover effect</verify>
  <done>Task card component with click-to-view-details</done>
</task>

<task type="auto">
  <name>Task 3: Create TaskDetail component</name>
  <files>frontend-v2/src/components/tasks/TaskDetail.tsx</files>
  <action>
    Create frontend-v2/src/components/tasks/TaskDetail.tsx:

    **Props:**
    - task: Task

    **Display:**
    1. **Header:**
       - Task name (large, bold)
       - Task type badge
       - TaskStatusBadge
       - Action buttons (if applicable): "Run", "View Logs", etc.

    2. **Details Section:**
       - Description or prompt (if available)
       - Created date
       - Updated date (if different)
       - Dependencies list (if any): Shows linked tasks

    3. **Metadata:**
       - Agent or model used (if applicable)
       - Tokens consumed (if available)
       - Duration (if available)
       - Error message (if failed)

    4. **Output/Artifacts:**
       - List of artifacts produced by this task
       - Links to view artifacts

    5. **Actions:**
       - Primary action based on status:
         - pending: "Run Task" button
         - running: "View Logs" button
         - completed: "View Output" button
         - failed: "Retry" button
       - Secondary actions as needed

    **Styling:**
    - Use design tokens
    - Section-based layout with headings
    - Clean, organized display
    - Proper spacing between sections
    - Action buttons at bottom

    **Behavior:**
    - Read-only display of task details
    - Action buttons trigger appropriate handlers
    - Links navigate to artifacts or logs

    **Usage:**
    - Will be displayed in DetailsPanel when task is clicked
    - Also could be used in modal or dedicated page

    This component provides comprehensive task information for users to understand and manage tasks.
  </action>
  <verify>TaskDetail displays all task information and actions</verify>
  <done>Task detail component with full information display</done>
</task>

<task type="auto">
  <name>Task 4: Create ProjectWorkspace page</name>
  <files>frontend-v2/src/pages/ProjectWorkspace.tsx</files>
  <action>
    Create frontend-v2/src/pages/ProjectWorkspace.tsx:

    **Route:** /project/:projectId

    **Layout:** Use WorkspaceLayout component

    **State:**
    - project: Project | null
    - tasks: Task[]
    - isLoading: boolean
    - error: string | null
    - activeView: 'overview' | 'tasks' | 'artifacts' | 'logs' (from useUIStore)

    **Component Structure:**
    1. **Header:**
       - Project name (large)
       - Research goal (below name)
       - Status badge for project
       - Action buttons (if applicable): "Pause", "Resume", etc.

    2. **Navigation Tabs:**
       - Overview
       - Tasks
       - Artifacts
       - Logs
       - Click to switch activeView in useUIStore
       - Active tab highlighted

    3. **Main Content (based on activeView):**

       **Overview View:**
       - Project summary card
       - Task counts (total, completed, running, failed)
       - Progress bar or chart
       - Recent activity

       **Tasks View:**
       - Filter bar (all, pending, running, completed, failed)
       - Sort dropdown (by date, status, name)
       - Grid or list of TaskCard components
       - Click task → open DetailsPanel with TaskDetail

       **Artifacts View:**
       - Filter by artifact type
       - Grid/list of artifacts
       - (Will be implemented in Phase 03)

       **Logs View:**
       - Log viewer
       - (Will be implemented in Phase 04)

    **Data Fetching:**
    - useEffect on mount: Get projectId from URL params
    - Fetch project details via getProject(projectId)
    - Fetch tasks via getProjectTasks(projectId) (create this method)
    - Update useProjectStore with active project
    - Handle loading and error states

    **Task Selection:**
    - Track selected task in local state or useUIStore
    - When task clicked: Open DetailsPanel with TaskDetail
    - Pass task to TaskDetail component

    **Error Handling:**
    - Handle 404 (project not found)
    - Handle permission errors
    - Show error message or redirect

    **Styling:**
    - Use design tokens throughout
    - Clean, organized layout
    - Responsive task grid
    - Proper spacing and hierarchy

    **Navigation Integration:**
    - Sidebar navigation already exists (from Phase 01)
    - Set active view via useUIStore.setActiveView()
    - Sidebar highlights active tab

    This is the main workspace where users manage their research projects.
  </action>
  <verify>ProjectWorkspace page displays project info and task list</verify>
  <done>Project workspace page with navigation and task management</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Project Workspace page with navigation tabs and task list</what-built>
  <how-to-verify>
    1. Run: cd frontend-v2 && npm start
    2. Navigate to a project (e.g., from Home Dashboard, click a project)
    3. Verify: ProjectWorkspace page loads
    4. Check: Project name displays at top
    5. Check: Research goal displays below name
    6. Click through navigation tabs (Overview, Tasks, Artifacts, Logs)
    7. Verify: Active tab highlights
    8. Go to Tasks view
    9. Verify: Task cards display in list/grid
    10. Verify: Status badges show correct colors
    11. Click a task card
    12. Verify: DetailsPanel opens with task details
    13. Check: TaskDetail shows all information
    14. Verify styling: Design tokens applied, consistent with theme
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] TaskStatusBadge displays all statuses correctly
- [ ] TaskCard shows task info with hover and click
- [ ] TaskDetail displays comprehensive task information
- [ ] ProjectWorkspace page manages views and tasks
- [ ] Navigation tabs switch between views
- [ ] Task list displays with filtering
- [ ] Clicking task opens DetailsPanel
- [ ] Visual verification passed
- [ ] npm run build succeeds
</verification>

<success_criteria>

- Task display components (TaskStatusBadge, TaskCard, TaskDetail)
- Project Workspace page with tabbed navigation
- Task list with filtering and status badges
- Task selection opens DetailsPanel
- Navigation integration with Sidebar
- Design tokens applied throughout
- Visual confirmation
</success_criteria>

<output>
After completion, create `frontend-v2/.planning/phases/02-core-screens/02-03-SUMMARY.md`
</output>
