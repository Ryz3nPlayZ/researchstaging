---
phase: 17-websocket-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend3/lib/websocket.ts
  - frontend3/App.tsx
autonomous: true

must_haves:
  truths:
    - "WebSocket connects to ws://localhost:8000/ws/{projectId} (not port 3000)"
    - "Connection URL derived from VITE_API_URL environment variable"
    - "Console logging reduced (state changes only, not every retry)"
    - "App continues working when WebSocket connection fails"
    - "Status indicator (Live/Connecting/Offline) reflects connection state"
    - "Auto-reconnect persists indefinitely with 3-second delay"
  artifacts:
    - path: "frontend3/lib/websocket.ts"
      provides: "WebSocket connection manager"
      contains: "VITE_API_URL"
    - path: "frontend3/App.tsx"
      provides: "WebSocket integration with ProjectContext"
      contains: "VITE_API_URL"
  key_links:
    - from: "frontend3/App.tsx"
      to: "VITE_API_URL"
      via: "import.meta.env.VITE_API_URL"
      pattern: "import\\.meta\\.env\\.VITE_API_URL"
    - from: "frontend3/lib/websocket.ts"
      to: "ws://localhost:8000/ws/{projectId}"
      via: "URL construction from VITE_API_URL"
      pattern: "replace.*http.*ws"
---

<objective>
Fix WebSocket connection to use correct backend port (8000) instead of frontend port (3000). The WebSocket currently connects through Vite proxy which doesn't handle WebSocket upgrades correctly. Change to direct backend connection using VITE_API_URL environment variable.

Purpose: Restore real-time features (task updates, document collaboration) that are currently broken due to incorrect WebSocket URL.
Output: Working WebSocket connection to backend with reduced console noise.
</objective>

<execution_context>
@/home/zemul/.claude/get-shit-done/workflows/execute-plan.md
@/home/zemul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-websocket-fixes/17-CONTEXT.md
@.planning/STATE.md
@.planning/ROADMAP.md

# User decisions (locked - must implement exactly):
- Direct to backend (not through Vite proxy)
- URL derived from VITE_API_URL environment variable
- Reduce console logging for reconnection attempts
- Keep auto-reconnect logic (3-second delay)
- Keep ping/pong heartbeat (30-second interval)
- No user-facing error messages for connection failures
- App works offline when WebSocket fails
</context>

<tasks>

<task type="auto">
  <name>Fix WebSocket URL to use direct backend connection from VITE_API_URL</name>
  <files>frontend3/lib/websocket.ts</files>
  <action>
    In frontend3/lib/websocket.ts, modify the connect() method:

    1. Remove the `baseUrl` parameter from the `connect()` function signature
    2. Construct the WebSocket URL directly from `import.meta.env.VITE_API_URL`:
       - Read `import.meta.env.VITE_API_URL` (default to 'http://localhost:8000' if undefined)
       - Replace 'http://' with 'ws://', 'https://' with 'wss://'
       - Append '/ws/' + projectId
    3. Resulting URL should be: `ws://localhost:8000/ws/{projectId}`

    Pattern to implement:
    ```typescript
    const apiUrl = import.meta.env.VITE_API_URL || 'http://localhost:8000';
    const wsUrl = apiUrl.replace('http://', 'ws://').replace('https://', 'wss://');
    const ws = new WebSocket(`${wsUrl}/ws/${projectId}`);
    ```

    Do NOT modify:
    - Auto-reconnect logic (3-second delay in onclose)
    - Ping/pong heartbeat (30-second interval)
    - Connection status tracking
  </action>
  <verify>grep -n "VITE_API_URL" frontend3/lib/websocket.ts returns the URL construction line</verify>
  <done>WebSocket URL constructed from VITE_API_URL, not from passed baseUrl parameter</done>
</task>

<task type="auto">
  <name>Update App.tsx to use VITE_API_URL and remove window.location.origin</name>
  <files>frontend3/App.tsx</files>
  <action>
    In frontend3/App.tsx, modify the WebSocketWrapper component:

    1. Remove the second parameter from the `connect()` call (line 25)
    2. Change from: `connect(currentProjectId, window.location.origin)`
    3. Change to: `connect(currentProjectId)`

    The WebSocketManager now handles URL construction internally using VITE_API_URL.
  </action>
  <verify>grep -n "connect(currentProjectId" frontend3/App.tsx shows single-parameter call</verify>
  <done>App.tsx passes only projectId to connect(), no baseUrl parameter</done>
</task>

<task type="auto">
  <name>Reduce console logging for WebSocket reconnection attempts</name>
  <files>frontend3/lib/websocket.ts</files>
  <action>
    In frontend3/lib/websocket.ts, reduce console noise:

    1. Keep console.log for state changes:
       - "WebSocket connected for project: {id}" (onopen)
       - "WebSocket closed for project: {id}" (onclose)

    2. Remove console.log for reconnection attempts:
       - Remove or comment out any log inside the onclose reconnect timeout
       - The status indicator is sufficient for user feedback

    3. Keep console.error for actual errors:
       - "WebSocket parse error:" (onmessage)
       - "WebSocket error:" (onerror)

    Result: Only state changes log, not every retry attempt (retries happen every 3 seconds indefinitely).
  </action>
  <verify>grep -n "console.log" frontend3/lib/websocket.ts shows only connection state messages, not retry logs</verify>
  <done>Console logs only on connection state changes (open/close), not on every reconnection attempt</done>
</task>

</tasks>

<verification>
1. Start backend server on port 8000
2. Start frontend3 with `npm run dev`
3. Open browser to http://localhost:3000
4. Open browser DevTools Console
5. Navigate to Editor view or any view that loads a project
6. Verify console shows: "WebSocket connected for project: {id}"
7. Verify URL in browser Network > WS tab shows: ws://localhost:8000/ws/{projectId}
8. Verify status indicator in editor toolbar shows "Live" (green dot)
9. Stop backend server to simulate connection failure
10. Verify status indicator changes to "Offline" (red dot)
11. Verify no spam of reconnection logs in console
12. Restart backend server
13. Verify WebSocket reconnects automatically (status returns to "Live")
</verification>

<success_criteria>
1. WebSocket connects to ws://localhost:8000/ws/{projectId} (confirmed via DevTools Network tab)
2. VITE_API_URL environment variable is used for URL construction
3. Console logs only state changes, not retry attempts
4. App continues working when WebSocket is disconnected
5. Status indicator accurately reflects connection state
6. Auto-reconnect works when backend becomes available again
</success_criteria>

<output>
After completion, create `.planning/phases/17-websocket-fixes/17-01-SUMMARY.md`
</output>
