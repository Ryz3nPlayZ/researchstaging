---
phase: 10-frontend-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [frontend3/index.html]
autonomous: true
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Tailwind CSS loads and applies styles correctly"
    - "Material Symbols icons display correctly (outlined and filled variants)"
    - "Primary color #4a8fe3 is defined and used in UI"
    - "Dark mode support works via Tailwind darkMode: 'class'"
    - "Custom scrollbar styling appears"
  artifacts:
    - path: "frontend3/index.html"
      provides: "Tailwind CSS CDN configuration and Material Symbols font loading"
      contains: "tailwind.config"
      contains: "material-symbols-outlined"
      contains: "primary: '#4a8fe3'"
  key_links:
    - from: "frontend3/index.html"
      to: "Tailwind CDN"
      via: "script src tag"
      pattern: "cdn.tailwindcss.com"
    - from: "frontend3/index.html"
      to: "Material Symbols"
      via: "Google Fonts link tag"
      pattern: "fonts.googleapis.com.*Material.*Symbols"
---

<objective>
Verify Tailwind CSS and Material Symbols integration is properly configured.

Purpose: Confirm that the existing Tailwind CSS configuration (via CDN) and Material Symbols icons from researchai-workspace.zip work correctly. This is a verification plan, not implementation.

Output: Confirmed working Tailwind CSS with custom theme and Material Symbols icons.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Tailwind and Material Symbols configuration:]
@frontend3/index.html
@frontend3/App.tsx
@frontend3/components/Sidebar.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify Tailwind CSS configuration in index.html</name>
  <files>frontend3/index.html</files>
  <action>
    Check that Tailwind CSS is properly configured via CDN:
    ```bash
    grep -n "tailwind" frontend3/index.html
    ```

    Verify these elements exist:
    1. CDN script tag: `<script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio"></script>`
    2. Inline tailwind.config object with:
       - darkMode: 'class'
       - primary color: '#4a8fe3'
       - background colors: { light: '#f6f7f8', dark: '#121820' }
       - fontFamily: { sans: ['Inter', 'sans-serif'] }
    3. Custom scrollbar CSS in <style> tag
    4. Material Symbols font variation settings

    Do NOT modify index.html. The existing configuration is correct.
  </action>
  <verify>
    Run: grep -c "tailwind.config" frontend3/index.html
    Expected: Output is 1 (tailwind.config object exists)
  </verify>
  <done>
    Tailwind CSS configuration verified in index.html with custom theme
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Material Symbols integration</name>
  <files>frontend3/index.html, frontend3/components/Sidebar.tsx</files>
  <action>
    Check Material Symbols font loading and usage:

    1. Verify Google Fonts link in index.html:
       ```bash
       grep "Material+Symbols" frontend3/index.html
       ```
       Expected: Link to fonts.googleapis.com with Material+Symbols+Outlined

    2. Verify CSS classes for icon variants:
       ```bash
       grep -A2 "material-symbols" frontend3/index.html | head -10
       ```
       Expected: .material-symbols-outlined and .material-symbols-filled classes with font-variation-settings

    3. Verify icon usage in components:
       ```bash
       grep -r "material-symbols-outlined" frontend3/*.tsx | wc -l
       ```
       Expected: Multiple instances (at least 10)

    Do NOT modify any files. Material Symbols are already properly integrated.
  </action>
  <verify>
    Run: grep -c "material-symbols-outlined" frontend3/components/Sidebar.tsx
    Expected: At least 5 (icons in sidebar navigation)
  </verify>
  <done>
    Material Symbols font linked and used throughout components
  </done>
</task>

<task type="auto">
  <name>Task 3: Create visual verification test file</name>
  <files>frontend3/TEST_STYLES.html</files>
  <action>
    Create a test file to verify Tailwind and Material Symbols render correctly:

    ```bash
    cat > frontend3/TEST_STYLES.html << 'EOF'
    <!DOCTYPE html>
    <html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Style Test</title>
        <script src="https://cdn.tailwindcss.com?plugins=forms,typography,aspect-ratio"></script>
        <link href="https://fonts.googleapis.com/css2?family=Inter:wght@100..900&display=swap" rel="stylesheet">
        <link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" rel="stylesheet" />
        <script>
            tailwind.config = {
                darkMode: 'class',
                theme: {
                    extend: {
                        colors: {
                            primary: '#4a8fe3',
                            background: {
                                light: '#f6f7f8',
                                dark: '#121820'
                            }
                        },
                        fontFamily: {
                            sans: ['Inter', 'sans-serif'],
                        },
                    }
                }
            }
        </script>
        <style>
            .material-symbols-outlined {
                font-variation-settings: 'FILL' 0, 'wght' 400, 'GRAD' 0, 'opsz' 24;
            }
            .material-symbols-filled {
                font-variation-settings: 'FILL' 1, 'wght' 400, 'GRAD' 0, 'opsz' 24;
            }
        </style>
    </head>
    <body class="bg-background-light text-slate-900 font-sans p-8">
        <h1 class="text-2xl font-bold text-primary mb-4">Tailwind & Material Symbols Test</h1>

        <div class="space-y-4">
            <div class="flex items-center gap-2">
                <span class="material-symbols-outlined text-primary">dashboard</span>
                <span>Outlined icon (primary color)</span>
            </div>
            <div class="flex items-center gap-2">
                <span class="material-symbols-filled text-primary">auto_awesome</span>
                <span>Filled icon (primary color)</span>
            </div>
            <div class="bg-white p-4 rounded-lg shadow">White card</div>
            <div class="bg-primary text-white p-4 rounded-lg">Primary color card</div>
        </div>

        <script>
            // Test dark mode toggle
            document.body.classList.toggle('dark');
            setTimeout(() => {
                console.log('Dark mode test: Background should be dark');
                document.body.classList.toggle('dark');
            }, 2000);
        </script>
    </body>
    </html>
    EOF
    ```

    This test file verifies:
    - Tailwind CDN loads
    - Primary color (#4a8fe3) applies
    - Material Symbols render (outlined and filled)
    - Background colors work
    - Dark mode class toggling works
  </action>
  <verify>
    Run: ls -la frontend3/TEST_STYLES.html
    Expected: TEST_STYLES.html file exists
  </verify>
  <done>
    Visual verification test file created at frontend3/TEST_STYLES.html
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Tailwind CSS configuration verified in index.html
- [ ] Material Symbols font linked and used
- [ ] TEST_STYLES.html created with all visual elements
- [ ] Primary color #4a8fe3 confirmed in tailwind.config
</verification>

<success_criteria>

- All tasks completed
- Tailwind CSS configuration documented
- Material Symbols integration verified
- Test file created for visual confirmation
- No modifications to existing setup (verification only)
  </success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-foundation/10-02-SUMMARY.md`
</output>
