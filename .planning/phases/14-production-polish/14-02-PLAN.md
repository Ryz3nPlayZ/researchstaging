---
phase: 14-production-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend3/components/ErrorBoundary.tsx
  - frontend3/components/LoadingSpinner.tsx
  - frontend3/App.tsx
  - frontend3/pages/DashboardView.tsx
  - frontend3/pages/LibraryView.tsx
  - frontend3/pages/AnalysisView.tsx
  - frontend3/pages/MemoryView.tsx
autonomous: true
must_haves:
  truths:
    - "React Error Boundary wraps the entire app"
    - "All async views show loading state during data fetch"
    - "API errors display user-friendly error messages"
    - "Loading states use consistent visual design"
  artifacts:
    - path: "frontend3/components/ErrorBoundary.tsx"
      provides: "React Error Boundary component for catching runtime errors"
      min_lines: 30
    - path: "frontend3/components/LoadingSpinner.tsx"
      provides: "Reusable loading spinner component"
      min_lines: 20
    - path: "frontend3/App.tsx"
      contains: "ErrorBoundary"
  key_links:
    - from: "App.tsx"
      to: "ErrorBoundary"
      via: "import and component wrapping"
    - from: "pages/*View.tsx"
      to: "LoadingSpinner"
      via: "conditional rendering during loading state"
---

<objective>
Add error boundaries and consistent loading states across all views for better user experience.

Purpose: Prevent app crashes from runtime errors and provide visual feedback during async operations.
Output: Error boundary component, loading spinner component, and integration across all views.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@frontend3/App.tsx
@frontend3/pages/DashboardView.tsx
@frontend3/pages/FilesView.tsx
@frontend3/pages/LibraryView.tsx
@frontend3/pages/AnalysisView.tsx
@frontend3/pages/MemoryView.tsx
@frontend3/pages/EditorView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ErrorBoundary component with fallback UI</name>
  <files>frontend3/components/ErrorBoundary.tsx</files>
  <action>
    Create `components/ErrorBoundary.tsx` as a React class component (Error Boundaries must be class components):
    - Implements `componentDidCatch(error, errorInfo)` lifecycle method
    - Implements `getDerivedStateFromError()` to set hasError state
    - Provides user-friendly error fallback UI with:
      - Error icon (Material Symbols "error" icon)
      - "Something went wrong" heading
      - Option to retry (reload page or reset error boundary)
      - Error details collapsed in details element (for debugging)
    - Accepts `children` prop
    - Optional: `fallback` prop for custom error UI
    - Logs errors to console.error for development debugging

    Follow React 19 Error Boundary patterns.
  </action>
  <verify>
    - ErrorBoundary.tsx is a valid React class component
    - Implements componentDidCatch and getDerivedStateFromError
    - Exports default ErrorBoundary component
  </verify>
  <done>ErrorBoundary component created and exported</done>
</task>

<task type="auto">
  <name>Task 2: Create reusable LoadingSpinner component</name>
  <files>frontend3/components/LoadingSpinner.tsx</files>
  <action>
    Create `components/LoadingSpinner.tsx` as a functional component:
    - Accepts optional props: `size` (sm/md/lg), `text` (optional message)
    - Uses Material Symbols animation with CSS spin
    - Consistent with existing loading spinners (see FilesView.tsx line 156)
    - Default size: medium (h-12 w-12)
    - Optional text displays below spinner
    - Returns JSX with centered flex container

    Style matches project design system (primary color, dark mode support).
  </action>
  <verify>
    - LoadingSpinner.tsx is a valid React functional component
    - Component accepts size and text props
    - Matches visual style of existing loading indicators
  </verify>
  <done>LoadingSpinner component created and exported</done>
</task>

<task type="auto">
  <name>Task 3: Integrate ErrorBoundary and LoadingSpinner across all views</name>
  <files>frontend3/App.tsx, frontend3/pages/DashboardView.tsx, frontend3/pages/LibraryView.tsx, frontend3/pages/AnalysisView.tsx, frontend3/pages/MemoryView.tsx</files>
  <action>
    **App.tsx:**
    - Import ErrorBoundary from './components/ErrorBoundary'
    - Wrap the entire app return in `<ErrorBoundary>`

    **For each view (Dashboard, Library, Analysis, Memory):**
    - Import LoadingSpinner from '../components/LoadingSpinner'
    - If view doesn't have loading state, add useState for loading (default true)
    - Replace any ad-hoc loading JSX with `<LoadingSpinner />`
    - Ensure loading state is set to false after API call completes (finally block)
    - Ensure error state displays user-friendly error message (not just console.error)

    **FilesView and EditorView:**
    - These already have good loading states (verify and keep as-is)
    - Just ensure error states show user-friendly messages (replace alert() if used for errors)

    All views should show loading state during initial data fetch and show error message on API failure.
  </action>
  <verify>
    - App.tsx wraps children in ErrorBoundary
    - All async views show LoadingSpinner during data fetch
    - All views have error state with user-friendly message
    - No bare console.error without UI feedback
  </verify>
  <done>ErrorBoundary wraps app, all views have loading and error states</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ErrorBoundary.tsx exists and is a valid class component
- [ ] LoadingSpinner.tsx exists with size and text props
- [ ] App.tsx imports and uses ErrorBoundary wrapper
- [ ] DashboardView, LibraryView, AnalysisView, MemoryView show loading state
- [ ] All views have error state with user-friendly message
- [ ] TypeScript compiles without errors: `npm run build`
</verification>

<success_criteria>

- Error boundary catches runtime errors and shows fallback UI
- All async views display loading state during data fetch
- API errors show user-friendly error messages
- Loading visual design is consistent across views
</success_criteria>

<output>
After completion, create `.planning/phases/14-production-polish/14-02-SUMMARY.md` documenting:
- ErrorBoundary implementation details
- LoadingSpinner component API
- List of views updated with loading states
- Any edge cases handled (e.g., what happens on network errors)
</output>
