---
phase: 02-core-screens
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-v2/src/services/planning.ts
  - frontend-v2/src/components/chat/ChatInterface.tsx
  - frontend-v2/src/components/chat/MessageBubble.tsx
  - frontend-v2/src/components/chat/PlanReviewCard.tsx
  - frontend-v2/src/pages/ConversationalPlanning.tsx
autonomous: false

must_haves:
  truths:
    - "User can see initial message from router agent"
    - "User can type responses in chat interface"
    - "User can see chat history in scrollable list"
    - "User can see generated plan summary card"
    - "User can confirm or revise plan"
    - "Confirming plan creates project and navigates to workspace"
  artifacts:
    - path: "frontend-v2/src/services/planning.ts"
      provides: "Planning API service"
      exports: ["startPlanning", "sendMessage", "generatePlan"]
    - path: "frontend-v2/src/components/chat/ChatInterface.tsx"
      provides: "Chat UI with message list and input"
      min_lines: 80
    - path: "frontend-v2/src/components/chat/MessageBubble.tsx"
      provides: "Individual chat message display"
      min_lines: 40
    - path: "frontend-v2/src/components/chat/PlanReviewCard.tsx"
      provides: "Plan summary display with confirm/revise"
      min_lines: 50
    - path: "frontend-v2/src/pages/ConversationalPlanning.tsx"
      provides: "Conversational planning flow page"
      min_lines: 100
  key_links:
    - from: "ConversationalPlanning.tsx"
      to: "planning.ts"
      via: "startPlanning on mount, sendMessage on user input"
    - from: "ChatInterface.tsx"
      to: "MessageBubble.tsx"
      via: "map over messages array"
    - from: "PlanReviewCard.tsx"
      to: "projects.ts"
      via: "createProject API call on confirm"
---

<objective>
Build Conversational Planning Flow - chat interface where Router Agent gathers context through iterative clarification before generating the research plan.

Purpose: Replace complex form with natural conversation. User describes research goal, AI asks clarifying questions, then generates a structured plan for review and confirmation.

Output: Chat-based planning flow with message history, plan review card, and project creation.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@frontend-v2/.planning/PROJECT.md
@frontend-v2/.planning/ROADMAP.md
@frontend-v2/.planning/STATE.md
@.planning/UI_UX_SPECIFICATION.md
@frontend-v2/src/types/project.ts
@frontend-v2/src/stores/useProjectStore.ts
@frontend-v2/src/services/api.ts
@frontend-v2/src/components/layout/WorkspaceLayout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create planning service layer</name>
  <files>frontend-v2/src/services/planning.ts</files>
  <action>
    Create frontend-v2/src/services/planning.ts for planning API operations:

    1. Import apiClient from services/api.ts
    2. Import types from types/project.ts

    3. Implement startPlanning(researchGoal: string): Promise<PlanningSession>
       - POST /api/planning/start
       - Request: { research_goal }
       - Returns: { session_id, initial_message, conversation_id }

    4. Implement sendMessage(sessionId: string, message: string): Promise<PlanningResponse>
       - POST /api/planning/{sessionId}/message
       - Request: { message }
       - Returns: { response, is_complete, plan_summary (if complete) }

    5. Implement generatePlan(sessionId: string): Promise<ResearchPlan>
       - POST /api/planning/{sessionId}/generate
       - Returns: { plan_id, phases, tasks, estimated_credits }

    Define TypeScript types:
    - PlanningSession: { session_id, initial_message, conversation_id }
    - PlanningResponse: { response: string, is_complete: boolean, plan_summary?: PlanSummary }
    - PlanSummary: { research_goal, output_type, audience, phases: string[] }
    - ResearchPlan: { plan_id, phases: Phase[], tasks: Task[], estimated_credits: number }

    All methods should use apiClient with proper error handling.
  </action>
  <verify>planning.ts exists with all planning API methods</verify>
  <done>Planning service layer with chat and plan generation</done>
</task>

<task type="auto">
  <name>Task 2: Create MessageBubble component</name>
  <files>frontend-v2/src/components/chat/MessageBubble.tsx</files>
  <action>
    Create frontend-v2/src/components/chat/MessageBubble.tsx:

    **Props:**
    - message: string
    - role: 'user' | 'assistant' | 'system'
    - timestamp?: string

    **Display:**
    1. **User messages:**
       - Right-aligned
       - Primary color background (navy #0f172a)
       - White text
       - Rounded corners (pill shape)
       - Max-width 70%

    2. **Assistant messages:**
       - Left-aligned
       - Gray background (bg-secondary)
       - Dark text (text-primary)
       - Rounded corners
       - Max-width 70%

    3. **System messages (if any):**
       - Center-aligned
       - Small, muted text
       - Italic
       - No background

    **Styling:**
    - Use design tokens for colors
    - Smooth transitions
    - Proper spacing between messages
    - Timestamp below message (small, muted)

    **Behavior:**
    - Render markdown if assistant message (optional, can use simple text for now)
    - Handle long messages with proper wrapping
    - No overflow issues

    This component will be used by ChatInterface to render each message in the conversation.
  </action>
  <verify>MessageBubble renders user and assistant messages correctly</verify>
  <done>Chat message bubble component with role-based styling</done>
</task>

<task type="auto">
  <name>Task 3: Create ChatInterface component</name>
  <files>frontend-v2/src/components/chat/ChatInterface.tsx</files>
  <action>
    Create frontend-v2/src/components/chat/ChatInterface.tsx:

    **Props:**
    - messages: Array<{ role, content, timestamp }>
    - onSendMessage: (message: string) => void
    - isLoading?: boolean

    **Layout:**
    1. **Header (optional):**
       - Title: "Plan your research"
       - Or no header (just chat interface)

    2. **Message List:**
       - Scrollable container
       - Map over messages array
       - Render MessageBubble for each
       - Auto-scroll to bottom on new messages
       - Loading indicator if isLoading

    3. **Input Area:**
       - Textarea or input at bottom
       - Multi-line support (for longer responses)
       - Send button on right
       - Disabled while loading
       - Submit on Ctrl+Enter or Click Send
       - Auto-focus on mount

    **State:**
    - Local input value state
    - Handle onChange to update input
    - Handle onSubmit to call onSendMessage(input)
    - Clear input after send

    **Styling:**
    - Flex column layout (messages grow, input fixed at bottom)
    - Use design tokens
    - Clean, minimal design
    - Max-width with centering
    - Full height with proper overflow

    **Behavior:**
    - Auto-scroll to bottom when new messages arrive
    - Focus input on mount
    - Handle Enter key (submit) vs Shift+Enter (newline)

    This is a reusable chat component that will be used in ConversationalPlanning page.
  </action>
  <verify>ChatInterface renders messages and handles input</verify>
  <done>Chat interface component with message list and input</done>
</task>

<task type="auto">
  <name>Task 4: Create PlanReviewCard component</name>
  <files>frontend-v2/src/components/chat/PlanReviewCard.tsx</files>
  <action>
    Create frontend-v2/src/components/chat/PlanReviewCard.tsx:

    **Props:**
    - planSummary: { research_goal, output_type, audience, phases: string[] }
    - onConfirm: () => void
    - onRevise: () => void
    - estimatedCredits?: number

    **Display:**
    1. **Header:**
       - Title: "Your Research Plan"
       - Or: "Ready to start?"

    2. **Plan Summary:**
       - Research goal (formatted nicely)
       - Output type badge
       - Audience description
       - Phases list (bullet points)
       - Estimated credits (if provided)

    3. **Actions:**
       - Primary button: "Confirm & Start Research"
       - Secondary button: "Revise Plan"
       - Buttons side by side or stacked

    **Styling:**
    - Card layout with border
    - Use design tokens
    - Prominent confirm button (primary variant)
    - Subtle revise button (ghost or secondary variant)
    - Good spacing and hierarchy

    **Behavior:**
    - onConfirm: Triggers project creation
    - onRevise: Returns to chat for more conversation

    This component appears at the end of the conversation flow when planning is complete.
  </action>
  <verify>PlanReviewCard displays plan with action buttons</verify>
  <done>Plan review card component with confirm and revise actions</done>
</task>

<task type="auto">
  <name>Task 5: Create ConversationalPlanning page</name>
  <files>frontend-v2/src/pages/ConversationalPlanning.tsx</files>
  <action>
    Create frontend-v2/src/pages/ConversationalPlanning.tsx:

    **Route:** /plan

    **Layout:** Use WorkspaceLayout component

    **State:**
    - messages: Array of chat messages
    - sessionId: string | null
    - isLoading: boolean
    - planSummary: PlanSummary | null
    - researchGoal: string (from location state or navigation)

    **Component Structure:**
    1. **Main Content:**
       - ChatInterface component (full height/width)
       - PlanReviewCard (if planSummary exists)

    2. **Flow:**
       - On mount: Get research_goal from location.state
       - Call startPlanning(researchGoal)
       - Add initial assistant message to chat
       - Set sessionId

    3. **User sends message:**
       - Add user message to chat
       - Call sendMessage(sessionId, message)
       - Add assistant response to chat
       - Check if is_complete
       - If complete: Show PlanReviewCard

    4. **Confirm plan:**
       - Call generatePlan(sessionId) or use planSummary
       - Create project via createProject API
       - Navigate to /project/:id (ProjectWorkspace)

    5. **Revise plan:**
       - Hide PlanReviewCard
       - Return to chat interface
       - User can continue conversation

    **State Management:**
    - Local state for messages and session
    - Use useNavigate from react-router-dom
    - Use useLocation from react-router-dom

    **Styling:**
    - Full viewport height for chat
    - Clean, focused interface
    - PlanReviewCard overlays or appears below chat

    **Error Handling:**
    - Handle API errors gracefully
    - Show error messages in chat or toast
    - Retry logic for failed requests

    **Navigation:**
    - Receive research_goal from HomeDashboard (location.state)
    - Navigate to ProjectWorkspace on confirm
  </action>
  <verify>ConversationalPlanning page handles chat flow and plan review</verify>
  <done>Conversational planning page with chat and plan review</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Conversational Planning Flow with chat interface and plan review</what-built>
  <how-to-verify>
    1. Run: cd frontend-v2 && npm start
    2. Navigate to Home Dashboard, type research goal, click Start
    3. Verify: ConversationalPlanning page loads
    4. Verify: Initial message from assistant displays
    5. Type a message in input and click Send
    6. Verify: User message appears on right
    7. Verify: Assistant response appears (may be simulated if backend not ready)
    8. Verify: Messages auto-scroll to bottom
    9. When plan ready: Verify PlanReviewCard appears
    10. Check: Plan summary displays correctly
    11. Verify: "Confirm & Start Research" and "Revise Plan" buttons visible
    12. Check styling: Message bubbles have correct colors and alignment
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] planning.ts service created with all methods
- [ ] MessageBubble component displays user/assistant messages
- [ ] ChatInterface component handles messages and input
- [ ] PlanReviewCard displays plan summary with actions
- [ ] ConversationalPlanning page manages full flow
- [ ] Chat interface is functional (send messages, see responses)
- [ ] Plan review displays correctly
- [ ] Visual verification passed
- [ ] npm run build succeeds
</verification>

<success_criteria>

- Planning API service with chat and plan generation
- Chat components (MessageBubble, ChatInterface, PlanReviewCard)
- Conversational planning flow with message history
- Plan review with confirm/revise actions
- Navigation from home dashboard to planning flow
- Design tokens applied throughout
- Visual confirmation
</success_criteria>

<output>
After completion, create `frontend-v2/.planning/phases/02-core-screens/02-02-SUMMARY.md`
</output>
