---
phase: 01-authentication
plan: 02
type: execute
wave: 1
depends_on: ["01"]
files_modified:
  - frontend/src/App.js
autonomous: true

must_haves:
  truths:
    - "User session persists across browser refresh"
    - "User can sign out from any page"
  artifacts:
    - path: "frontend/src/App.js"
      provides: "AppContent component that consumes AuthContext"
      contains: "useAuth\\(\\)"
  key_links:
    - from: "frontend/src/App.js"
      to: "frontend/src/context/AuthContext.js"
      via: "useAuth() hook call"
      pattern: "const.*=.*useAuth\\(\\)"
---

<objective>
Fix AppContent to consume AuthContext, enabling session persistence and logout functionality.

Purpose: Close critical gap where AppContent uses auth variables but never calls useAuth() hook.
Output: Working session persistence and logout functionality.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication/01-01-VERIFICATION.md
@.planning/phases/01-authentication/01-01-SUMMARY.md
@frontend/src/App.js
@frontend/src/context/AuthContext.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add useAuth() hook call to AppContent</name>
  <files>frontend/src/App.js</files>
  <action>
    In the AppContent function (around line 24), add this line after the function definition:

    ```javascript
    const { loading, user, logout } = useAuth();
    ```

    This destructures the auth state from AuthContext, making loading, user, and logout available to the component.

    Location: After line 24 (after AppContent function opens, before any usage of these variables).

    Do NOT modify anything else - the rest of the code already uses these variables correctly, they were just undefined due to missing hook call.
  </action>
  <verify>
    - File compiles without errors
    - useAuth() is called in AppContent function
    - loading, user, logout are destructured from useAuth
  </verify>
  <done>AppContent now consumes AuthContext via useAuth() hook</done>
</task>

<task type="auto">
  <name>Task 2: Add route protection logic</name>
  <files>frontend/src/App.js</files>
  <action>
    In AppContent function, add route protection after useAuth() call.

    Find where the component determines which view to show. Add check:

    ```javascript
    // Redirect to login if not authenticated
    if (!loading && !user) {
      return <LoginPage />;
    }
    ```

    This ensures unauthenticated users see the login page instead of the main app.

    Place this after the useAuth() call, before the main return statement.

    Reference: OAuthCallback.js (line 52-53) shows similar pattern for protected routes.
  </action>
  <verify>
    - Route protection logic exists in AppContent
    - Unauthenticated state redirects to LoginPage
    - Loading state shows app while checking auth
  </verify>
  <done>Protected routes redirect unauthenticated users to login</done>
</task>

<task type="auto">
  <name>Task 3: Test session persistence and logout</name>
  <files>frontend/src/App.js, frontend/src/context/AuthContext.js</files>
  <action>
    Verify the implementation works by checking the code flow:

    1. Session persistence check:
       - AuthContext reads token from localStorage on mount (line 23)
       - Calls /api/auth/me to validate token
       - Sets user state if valid
       - AppContent respects loading state during check
       - AppContent shows app content if user exists

    2. Logout check:
       - StatusBar calls handleLogout (line 130)
       - handleLogout calls logout() from useAuth
       - AuthContext.logout() clears token from localStorage (line 85)
       - AuthContext.logout() clears user state (line 81-84)
       - AppContent redirects to LoginPage due to route protection

    No code changes needed for this task - just verify the flow is correct in the existing code.
  </action>
  <verify>
    - AuthContext has localStorage.getItem on mount
    - AuthContext has localStorage.removeItem in logout
    - AppContent has route protection check
    - Flow: token → validation → user state → show content OR redirect to login
  </verify>
  <done>Verified session persistence and logout flows are correctly wired</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] useAuth() hook called in AppContent function
- [ ] Route protection logic added (!loading && !user check)
- [ ] Code compiles without errors
- [ ] No undefined variable references
</verification>

<success_criteria>

- All tasks completed
- useAuth() called in AppContent
- Session persistence works (token → user state)
- Logout works (clears token, redirects to login)
- No runtime errors from undefined variables
  </success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-02-SUMMARY.md`
</output>
