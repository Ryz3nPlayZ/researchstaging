<analysis>**original_problem_statement:**
The user wants to build an AI-native research execution system. This system will take a high-level research intent and autonomously produce structured research outputs like literature reviews and papers. The core of the system is a stateful, event-driven architecture that treats research as a computational pipeline.

**PRODUCT REQUIREMENTS:**
- **UX**: An IDE-like research cockpit with a minimal, front-loaded UX. It should feature a global status bar, a navigator for projects/tasks, a central workspace for documents, and an inspector for metadata/provenance. The default theme should be light, with a dark mode toggle.
- **Onboarding**: A calm, empty-by-default project dashboard is the first screen. Creating a new project initiates a guided, LLM-driven planning flow.
- **Architecture**: A Python (FastAPI) backend with PostgreSQL for persistence and Redis for task queuing/caching. A task management system (e.g., Celery/Dramatiq) will execute work asynchronously. Real-time updates should be delivered via WebSockets.
- **Core Components**:
    - **Projects**: Top-level research efforts.
    - **Tasks**: A Directed Acyclic Graph (DAG) of work units with dependencies.
    - **Artifacts**: Outputs produced by tasks (summaries, drafts).
    - **Runs**: Individual task executions.
    - **Events**: Immutable logs for auditability.
- **Integrations**:
    - **LLMs**: OpenAI, Gemini models.
    - **Literature Search**: Semantic Scholar, arXiv.
    - **Document Handling**: PDF acquisition/parsing (PyMuPDF, pdfplumber), reference extraction.
    - **Export**: Pandoc for exporting to PDF, DOCX, Markdown.
- **Visualizations**:
    - **Agent Graph**: An n8n-style graph showing agent orchestration using React Flow.
    - **Task Dependency Graph**: A graph showing task dependencies.
- **UI Features**:
    - Resizable sidebars.
    - A rich text editor (TipTap) for document viewing and editing.

**User's preferred language**: English

**what currently exists?**
The agent has successfully implemented two major versions of the application. The first was a MongoDB-based MVP. The second version introduced a new dashboard, a guided planning flow, resizable panels, and task/agent graph visualizations with React Flow.

Currently, the agent has started a massive, user-requested architectural refactor to migrate the backend from MongoDB and SSE to a more advanced orchestration engine using **PostgreSQL, Redis, and WebSockets**. It has installed the necessary dependencies (, , , usage: websockets [--version | <uri>]) and has scaffolded the new directory structure and files for the database models, orchestration engine, workers, and WebSocket handler. The system is in a non-functional, half-migrated state.

**Last working item**:
-   **Last item agent was working**: The agent was in the initial phase of a major architectural overhaul. It had just created the foundational Python files for the new PostgreSQL-based backend, including database models (), the orchestration engine (), task workers (), and the WebSocket handler (). The immediate next step was to update the frontend to use WebSockets.
-   **Status**: IN PROGRESS
-   **Agent Testing Done**: N
-   **Which testing method agent to use?**: both
-   **User Testing Done**: N

**All Pending/In progress Issue list**:
-   **Issue 1**: The entire application is non-functional due to the ongoing architectural migration. The backend  is a mix of old MongoDB logic and new, unconnected PostgreSQL scaffolding. The frontend is still configured to use SSE and the old API structure. Priority: **P0**.

**In progress Task List**:
-   **Task 1**: Complete the backend migration to the new PostgreSQL/Redis architecture. Priority: **P0**.
-   **Task 2**: Update the frontend to communicate with the new backend via WebSockets. Priority: **P0**.

**Task Detail**:
-   **Task 1: Complete Backend Migration**
    -   **Where to resume**: Rewrite  to remove all MongoDB and SSE logic. It needs to be fully integrated with the new components:
        1.  **Database**: Use the SQLAlchemy models from  and the connection from  for all database operations. Create the initial database schema.
        2.  **Orchestration**: Wire the API endpoints to the new orchestration engine ().
        3.  **Workers**: Ensure the orchestration engine correctly schedules tasks for the worker layer ().
        4.  **Real-time**: Integrate the WebSocket endpoint () to broadcast state changes from the orchestration engine.
    -   **What will be achieved with this?**: A fully functional, stateful backend that matches the user's specified architecture, capable of running complex research pipelines.
    -   **Status**: IN PROGRESS
    -   **Should Test frontend/backend/both after fix?**: backend

-   **Task 2: Update Frontend for WebSocket Integration**
    -   **Where to resume**:
        1.  Replace the existing SSE implementation (EventSource) in the frontend with a WebSocket client.
        2.  Update components like , , and the graph components () to receive and display real-time data from the WebSocket connection.
        3.  Modify the API calls in  to align with the new backend endpoints.
    -   **What will be achieved with this?**: The frontend will correctly display real-time progress of research tasks orchestrated by the new backend.
    -   **Status**: NOT STARTED
    -   **Should Test frontend/backend/both after fix?**: frontend

**Upcoming and Future Tasks**
-   **Upcoming Tasks**:
    -   **P0**: Implement the full DAG execution model as described in the user's architecture document.
    -   **P1**: Implement failure handling and task retry mechanisms in the orchestration engine.
    -   **P1**: Implement PDF acquisition, parsing, and reference extraction tasks.
    -   **P1**: Implement document export functionality using Pandoc.
-   **Future Tasks**:
    -   **P2**: Add user authentication.
    -   **P2**: Enhance the rich text editor with more advanced AI actions (rewrite, expand, cite).
    -   **P2**: Allow project duplication and creation of templates.

**Completed work in this session**
-   **MVP v1 (MongoDB)**:
    -   Implemented a full-stack application with a FastAPI/MongoDB backend and a React frontend.
    -   Created a three-panel IDE-style layout (Navigator, Workspace, Inspector).
    -   Integrated LLMs and literature search APIs.
-   **Feature Expansion (v2)**:
    -   Added a new Project Dashboard as the landing page.
    -   Implemented a multi-step, guided Planning Flow for new projects.
    -   Added resizable panels for the main workspace layout.
    -   Implemented Task and Agent graph visualizations using React Flow ().
    -   Implemented a basic rich text editor for viewing documents.
    -   Set the default theme to light and fixed caching in the planning flow.
-   **Architecture Refactor (In Progress)**:
    -   Installed PostgreSQL, Redis, and WebSocket dependencies.
    -   Scaffolded the new backend architecture directories and files.

**Earlier issues found/mentioned but not fixed**
-   The click handler for project cards on the dashboard () was unreliable and had to be bypassed by clicking from the navigator. This may still be an issue.

**Known issue recurrence from previous fork**
-   None.

**Code Architecture**


**Key Technical Concepts**
-   **Backend**: FastAPI, PostgreSQL (new), Redis (new), SQLAlchemy, WebSockets (new)
-   **Frontend**: React, Tailwind CSS, React Flow
-   **Architecture**: State-Driven Orchestration, Directed Acyclic Graph (DAG) for task management.

**key DB schema**
-   ****: id, name, status, created_at, updated_at
-   ****: id, project_id, definition (JSON), created_at
-   ****: id, project_id, name, status, dependencies (JSON), input_artifacts (JSON), output_artifacts (JSON)
-   ****: id, project_id, task_id, name, content_path, metadata (JSON)
-   ****: id, task_id, status, start_time, end_time, logs

**changes in tech stack**
-   **Database**: Migrating from **MongoDB** to **PostgreSQL**.
-   **Caching/Queuing**: Introducing **Redis**.
-   **Real-time Communication**: Migrating from **Server-Sent Events (SSE)** to **WebSockets**.

**All files of reference**
*New Files (for architecture refactor):*
-   : Manages the SQLAlchemy database connection.
-   : Defines the new PostgreSQL schema using SQLAlchemy.
-   : Contains the core logic for the new DAG-based task orchestration.
-   : Defines the logic for workers that execute tasks.
-   : Handles WebSocket connections and real-time updates.

*Key Modified Files:*
-   : Currently being rewritten to integrate all the new architectural components.
-   : Updated with , , , usage: websockets [--version | <uri>].

**Areas that need refactoring**
The entire application is undergoing a major refactor. The highest priority is , which is currently a mix of old and new code and serves as the main integration point for the new architecture. Following that, the entire frontend needs to be refactored to use WebSockets instead of SSE for real-time data.

**key api endpoints**
-   : The existing REST endpoints need to be rewritten to support the new PostgreSQL backend.
-   : A new WebSocket endpoint needs to be implemented for real-time updates.

**Critical Info for New Agent**
You are in the middle of a massive, user-directed architectural migration. **Do not attempt to fix or work on the old MongoDB/SSE implementation.** Your sole focus should be to complete the migration to the new architecture as specified by the user in **Chat Message 215**.

**Your immediate steps are:**
1.  Completely rewrite  to integrate the new PostgreSQL database, orchestration engine, and WebSocket handler. Remove all legacy MongoDB code.
2.  After the backend is updated, refactor the frontend to replace its  (SSE) implementation with a  client that connects to the new  endpoint.
3.  Proceed systematically. The system is currently broken. Get the backend wired up first, then fix the frontend.

**documents and test reports created in this job**
-   
-   
-   

**Last 10 User Messages and any pending HUMAN messages**
1.  **User**: *Provides a detailed backend architecture document for an advanced orchestration engine.* (Chat Message 215)
2.  **Agent**: *Asks for clarification on the scope of the architectural changes.* (Chat Message 216)
3.  **User**: *Confirms to proceed with the full implementation: postgresql. we need the full advanced orchestration engine. yes websockets. yes plan immutability. we will implement everything said.* (Chat Message 218)
4.  **Agent**: *Acknowledges and begins the implementation.* (Chat Message 219)
5.  **Agent**: *Installs dependencies for PostgreSQL, Redis, etc.* (Chat Message 220)
6.  **Agent**: *Detects that PostgreSQL and Redis are not running.* (Chat Message 223)
7.  **Agent**: *Starts the PostgreSQL and Redis services.* (Chat Message 225)
8.  **Agent**: *Configures the PostgreSQL database and user roles.* (Chat Message 227)
9.  **Agent**: *Starts creating the new backend files for the database, orchestration engine, workers, and WebSockets.* (Chat Messages 228-241)
10. **Agent**: *Rewrites  (partially) and states its next step is to update the frontend for WebSockets.* (Chat Messages 242-243)

**Project Health Check:**
-   **Broken**: The application is in a non-functional state due to the in-progress architectural migration from MongoDB to PostgreSQL/Redis.

**3rd Party Integrations**
-   **OpenAI/Gemini**: LLM providers for content generation.
-   **Semantic Scholar / arXiv**: Literature search APIs.
-   **PostgreSQL**: New primary database for persistence.
-   **Redis**: New service for caching and task queuing.
-   **React Flow**: For graph visualizations on the frontend.
-   **Pandoc**: For document export.

**Testing status**
-   Testing was successfully performed on the previous version of the application (v2, MongoDB-based), with a 100% pass rate in the last run ().
-   The current version with the new architecture is completely untested and non-functional.

**Credentials to test flow:**
N/A

**What agent forgot to execute**
The agent has correctly laid out the plan for the architectural refactor. It has created the necessary files but has not yet completed the most critical step: integrating all the new backend components within . It also has not started the corresponding frontend refactor to switch from SSE to WebSockets. This isn't a case of forgetting, but rather being in the middle of a very large, multi-step task.</analysis>
