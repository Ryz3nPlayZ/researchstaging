---
phase: 11-view-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend3/lib/api.ts
  - frontend3/pages/DashboardView.tsx
  - frontend3/pages/FilesView.tsx
autonomous: true

must_haves:
  truths:
    - "Dashboard displays real project data from backend /api/projects endpoint"
    - "Files view displays actual files from backend /api/files endpoint"
    - "API calls replace all mock data in Dashboard and Files views"
  artifacts:
    - path: "frontend3/lib/api.ts"
      provides: "Project and file API client methods"
      exports: ["projectApi", "fileApi"]
      min_lines: 80
    - path: "frontend3/pages/DashboardView.tsx"
      provides: "Project list display from backend"
      contains: "useEffect.*apiRequest|projectApi"
    - path: "frontend3/pages/FilesView.tsx"
      provides: "File list display from backend"
      contains: "useEffect.*apiRequest|fileApi"
  key_links:
    - from: "DashboardView.tsx"
      to: "/api/projects"
      via: "projectApi.list() on mount"
      pattern: "useEffect.*\\[\\]\\)"
    - from: "FilesView.tsx"
      to: "/api/files"
      via: "fileApi.list() on mount"
      pattern: "useEffect.*\\[\\]\\)"
---

<objective>
Connect Dashboard and Files views to backend APIs, replacing mock data with real project and file data from the FastAPI backend.

**Purpose:** Enable Dashboard and Files views to display live data from the Research Pilot backend, establishing the API integration pattern for subsequent views.

**Output:**
- Extended API client with project and file endpoints
- DashboardView displaying real projects from database
- FilesView displaying real files from cloud storage
- Error handling and loading states for API failures
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Phase 10 context: frontend3 build system verified with Vite proxy to backend
@.planning/phases/10-frontend-foundation/10-03-SUMMARY.md

# Backend API endpoints
@backend/server.py
@backend/file_api.py
@frontend3/lib/api.ts
@frontend3/pages/DashboardView.tsx
@frontend3/pages/FilesView.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend API client with project and file methods</name>
  <files>frontend3/lib/api.ts</files>
  <action>
Add the following API methods to frontend3/lib/api.ts:

**Project API methods:**
- Ensure `projectApi.list()` exists and returns projects from `/api/projects`
- Ensure `projectApi.create(data)` exists for creating new projects
- Add proper TypeScript types for Project response (id, research_goal, output_type, status, created_at)

**File API methods:**
- Add `fileApi.list(projectId?: string)` - lists files from `/api/files` (with optional project filter)
- Add proper TypeScript types for File response (id, name, file_type, size_bytes, created_at, path)

**Error handling:**
- All methods should return ApiResponse<T> with data, error, and status fields
- Add console.error logging for API failures

**TypeScript types to add:**
```typescript
export interface Project {
  id: string;
  research_goal: string;
  output_type: string;
  status: string;
  created_at: string;
  updated_at: string;
}

export interface File {
  id: string;
  name: string;
  file_type: string;
  size_bytes: number;
  created_at: string;
  path?: string;
}
```

Do NOT modify the apiRequest wrapper function - it's already working from Phase 10.
  </action>
  <verify>
1. TypeScript compiles without errors: `cd frontend3 && npm run build`
2. No import errors in the file
3. All API methods exported correctly
  </verify>
  <done>
API client extended with projectApi.list/create and fileApi.list methods, TypeScript types defined for Project and File responses.
  </done>
</task>

<task type="auto">
  <name>Task 2: Connect DashboardView to backend projects API</name>
  <files>frontend3/pages/DashboardView.tsx</files>
  <action>
Replace mock project data with real API calls:

**State management:**
- Add useState for projects array, loading state, error state
- Add useState for selected project (to track current project context)

**API integration:**
- Add useEffect that calls `projectApi.list()` on component mount
- Store returned projects in state (handle loading, error, success states)
- Add error handling: display error message if API call fails
- Add loading skeleton or spinner while fetching

**Data mapping:**
- Map backend Project fields to frontend display:
  - research_goal → project description/title
  - created_at → "Edited X time ago" (use relative time)
  - status → display status indicator
  - output_type → icon/type indicator

**UI updates:**
- "Create Project" button should call `projectApi.create({ research_goal: "New Project", output_type: "paper" })`
- Add error toast/banner if API calls fail
- Keep existing Material Symbols icons and layout (just replace data source)

**Preserve existing UI:**
- Keep the grid layout, card design, and styling
- Keep Material Symbols icons
- Keep "Recent Projects" and "All Projects" sections
- Only replace the data source (mock array → API call)

Do NOT change the component structure or layout - only replace data fetching.
  </action>
  <verify>
1. Component mounts without errors
2. TypeScript compiles without errors
3. API call made to /api/projects on mount (check browser Network tab)
4. Projects display from backend data if available
5. Loading state shows while fetching
6. Error state shows if API fails
  </verify>
  <done>
DashboardView fetches and displays real projects from /api/projects endpoint on mount, with loading and error states. Create Project button wired to backend API.
  </done>
</task>

<task type="auto">
  <name>Task 3: Connect FilesView to backend files API</name>
  <files>frontend3/pages/FilesView.tsx</files>
  <action>
Replace mock file data with real API calls:

**State management:**
- Add useState for files array, loading state, error state
- Add useState for current project ID (to filter files by project)

**API integration:**
- Add useEffect that calls `fileApi.list(projectId)` on component mount
- Store returned files in state (handle loading, error, success states)
- Add error handling: display error message if API call fails
- Add loading skeleton while fetching

**Data mapping:**
- Map backend File fields to frontend display:
  - name → file name
  - file_type → icon (use existing getFileIcon logic)
  - size_bytes → human-readable size (convert bytes to MB/KB)
  - created_at → "Uploaded X time ago" (relative time)
  - path → file path for download/preview

**UI updates:**
- "Upload Files" button should trigger file upload (UI only for now - full upload in Phase 12)
- Add error toast/banner if API calls fail
- Keep existing drag-drop zone UI (keep placeholder, wire upload later)

**Preserve existing UI:**
- Keep the list/grid layout
- Keep Material Symbols file type icons
- Keep file card design with hover states
- Only replace the data source (mock array → API call)

**File type icon mapping:**
Ensure the existing getFileIcon function handles backend file types correctly:
- "pdf" → red description icon
- "docx" → blue article icon
- "csv" → green table_chart icon
- default → gray draft icon

Do NOT change the component structure or layout - only replace data fetching.
  </action>
  <verify>
1. Component mounts without errors
2. TypeScript compiles without errors
3. API call made to /api/files on mount (check browser Network tab)
4. Files display from backend data if available
5. Loading state shows while fetching
6. Error state shows if API fails
7. File type icons display correctly based on file_type field
  </verify>
  <done>
FilesView fetches and displays real files from /api/files endpoint on mount, with loading and error states. File type icons mapped correctly from backend file_type field.
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `cd frontend3 && npm run build` succeeds without TypeScript errors
- [ ] DashboardView loads projects from /api/projects (check browser Network tab)
- [ ] FilesView loads files from /api/files (check browser Network tab)
- [ ] Loading states display during API fetch
- [ ] Error states display if API calls fail
- [ ] No mock data remains in DashboardView or FilesView
</verification>

<success_criteria>

- All tasks completed
- TypeScript compilation passes
- Dashboard and Files views display live data from backend
- API calls successfully replace mock data
- Loading and error states implemented
</success_criteria>

<output>
After completion, create `.planning/phases/11-view-integration/11-01-SUMMARY.md`

**Summary format:**
- phase, plan, subsystem tags
- Dependency graph (requires/provides/affects)
- Tech stack added/patterns established
- Key files modified
- Duration and completion timestamp
- Task commits list
- Accomplishments
- Deviations from plan
- Issues encountered
- Next phase readiness

**Key accomplishments to document:**
- API client extended with project and file methods
- Dashboard and Files views connected to backend
- Loading and error states implemented
- Mock data removed from both views
</output>
