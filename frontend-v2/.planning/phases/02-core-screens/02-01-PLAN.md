---
phase: 02-core-screens
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend-v2/src/services/projects.ts
  - frontend-v2/src/components/common/CreditsDisplay.tsx
  - frontend-v2/src/pages/HomeDashboard.tsx
  - frontend-v2/src/App.tsx
autonomous: false

must_haves:
  truths:
    - "User can see greeting with their name"
    - "User can see credits remaining display"
    - "User can type research goal in centered input"
    - "User can click 'New Project' button"
    - "User can see recent projects list"
    - "Submitting research goal navigates to planning flow"
  artifacts:
    - path: "frontend-v2/src/services/projects.ts"
      provides: "Project API service layer"
      exports: ["getProjects", "createProject", "getProject"]
    - path: "frontend-v2/src/components/common/CreditsDisplay.tsx"
      provides: "Credits balance display component"
      min_lines: 30
    - path: "frontend-v2/src/pages/HomeDashboard.tsx"
      provides: "Home dashboard page with input and project list"
      min_lines: 80
    - path: "frontend-v2/src/App.tsx"
      provides: "Routing setup for home dashboard"
      contains: "BrowserRouter, Routes, Route"
  key_links:
    - from: "HomeDashboard.tsx"
      to: "projects.ts"
      via: "createProject API call on form submit"
    - from: "CreditsDisplay.tsx"
      to: "useCreditStore"
      via: "useCreditStore hook for balance"
    - from: "HomeDashboard.tsx"
      to: "ConversationalPlanning"
      via: "react-router Navigate with research_goal state"
---

<objective>
Build Home Dashboard page - the main landing screen with research goal input, project list, and credits display.

Purpose: Create the entry point for users to start new research projects or continue existing ones. This is the first screen users see after login.

Output: Home Dashboard page with centered input, credits display, recent projects list, and navigation to planning flow.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@frontend-v2/.planning/PROJECT.md
@frontend-v2/.planning/ROADMAP.md
@frontend-v2/.planning/STATE.md
@.planning/UI_UX_SPECIFICATION.md
@frontend-v2/src/types/project.ts
@frontend-v2/src/stores/useCreditStore.ts
@frontend-v2/src/stores/useProjectStore.ts
@frontend-v2/src/stores/useUIStore.ts
@frontend-v2/src/components/layout/WorkspaceLayout.tsx
@frontend-v2/src/services/api.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create projects service layer</name>
  <files>frontend-v2/src/services/projects.ts</files>
  <action>
    Create frontend-v2/src/services/projects.ts for project API operations:

    1. Import apiClient from services/api.ts
    2. Import Project, CreateProjectRequest, CreateProjectResponse types
    3. Implement getProjects(): Promise<Project[]>
       - GET /api/projects
       - Returns array of user's projects

    4. Implement createProject(request: CreateProjectRequest): Promise<CreateProjectResponse>
       - POST /api/projects
       - Request: { research_goal, output_type, audience }
       - Returns: { project_id, status, message }

    5. Implement getProject(projectId: string): Promise<Project>
       - GET /api/projects/{projectId}
       - Returns full project details with tasks

    All methods should use apiClient with proper error handling.
  </action>
  <verify>projects.ts exists with getProjects, createProject, getProject methods</verify>
  <done>Project service layer with CRUD operations</done>
</task>

<task type="auto">
  <name>Task 2: Create CreditsDisplay component</name>
  <files>frontend-v2/src/components/common/CreditsDisplay.tsx</files>
  <action>
    Create frontend-v2/src/components/common/CreditsDisplay.tsx:

    **Props:** None (uses useCreditStore)

    **Display:**
    - Large credits remaining number: "1,000" (prominent)
    - Smaller text: "credits remaining"
    - Optional: "Used: X" in muted color
    - Use design token colors (primary, text-secondary)

    **State:**
    - Import useCreditStore
    - Display creditsRemaining from store
    - Optional: Display creditsUsed

    **Styling:**
    - Center-aligned or left-aligned based on container
    - Large font size for remaining credits (2-3rem)
    - Use design tokens for colors
    - Clean, minimal design

    **Usage:**
    - Will be used in HomeDashboard (sidebar or header)
    - Will be used in Settings page later
  </action>
  <verify>CreditsDisplay component renders credits from store</verify>
  <done>Credits display component showing balance</done>
</task>

<task type="auto">
  <name>Task 3: Create HomeDashboard page</name>
  <files>frontend-v2/src/pages/HomeDashboard.tsx</files>
  <action>
    Create frontend-v2/src/pages/HomeDashboard.tsx:

    **Route:** / (root path)

    **Layout:** Use WorkspaceLayout component

    **Components:**
    1. **Header:**
       - Greeting: "Good morning, [Name]" or "Welcome back"
       - Use useAuthStore to get user name/email
       - Time-based greeting (morning/afternoon/evening)

    2. **Centered Input Section:**
       - Large centered input box (pill or rounded rectangle)
       - Placeholder: "Describe your research goal..."
       - Submit button on right side: "Start" or arrow icon
       - Full width with max-width (600px)
       - On submit: Navigate to /plan with research_goal state

    3. **New Project Button:**
       - Alternative way to start: "New Project" button
       - Also navigates to /plan

    4. **Recent Projects Section:**
       - Below input: "Recent Projects" heading
       - Grid/list of recent projects (last 5)
       - Each project card shows:
         - Research goal (truncated)
         - Status badge
         - Created date
         - Click to navigate to /project/:id
       - If no projects: show empty state message

    **State Management:**
    - useEffect to fetch projects on mount
    - Use getProjects() from projects service
    - Store in local state
    - Loading state while fetching
    - Error handling

    **Styling:**
    - Use design tokens throughout
    - Center-aligned main content
    - Clean, minimal design
    - Hover effects on project cards
    - Responsive grid for projects

    **Navigation:**
    - On form submit: Navigate to /plan (ConversationalPlanning)
    - Pass research_goal as state or URL param
    - On project click: Navigate to /project/:id (ProjectWorkspace)
  </action>
  <verify>HomeDashboard renders with input, greeting, and project list</verify>
  <done>Home dashboard page with project creation flow</done>
</task>

<task type="auto">
  <name>Task 4: Set up routing in App.tsx</name>
  <files>frontend-v2/src/App.tsx</files>
  <action>
    Update frontend-v2/src/App.tsx to set up routing:

    1. Import BrowserRouter, Routes, Route from react-router-dom
    2. Import HomeDashboard from pages/HomeDashboard
    3. Set up routes:
       - Route path="/": HomeDashboard
       - (Other routes will be added in future plans)

    4. Remove/simplify existing content
    5. Wrap in BrowserRouter
    6. Add basic routing structure

    Note: This is minimal routing setup. Other pages (ConversationalPlanning, ProjectWorkspace) will be added in their respective plans.
  </action>
  <verify>App.tsx has BrowserRouter and Routes configured</verify>
  <done>Routing structure with home dashboard route</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Home Dashboard page with research goal input, credits display, and project list</what-built>
  <how-to-verify>
    1. Run: cd frontend-v2 && npm start
    2. Visit: http://localhost:3000
    3. Verify:
       - Greeting displays at top ("Good morning" or "Welcome back")
       - Centered input box is visible with placeholder text
       - Credits display shows balance (e.g., "1,000 credits remaining")
       - "Recent Projects" section displays below input
       - Typing in input and clicking Start keeps focus or shows feedback
       - Project cards (if any) display correctly with status badges
    4. Check styling: Navy theme (#0f172a) visible, clean minimal design
    5. Verify no console errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] projects.ts service created with all API methods
- [ ] CreditsDisplay component shows credits from store
- [ ] HomeDashboard page renders with all sections
- [ ] Routing configured in App.tsx
- [ ] Form submission navigates to /plan route
- [ ] Visual verification passed
- [ ] npm run build succeeds
</verification>

<success_criteria>

- Projects API service complete with getProjects, createProject, getProject
- Home Dashboard displays greeting, input, credits, and project list
- Form submission navigates to planning flow
- Credits display integrated with useCreditStore
- Design tokens applied throughout
- Visual confirmation of home dashboard
</success_criteria>

<output>
After completion, create `frontend-v2/.planning/phases/02-core-screens/02-01-SUMMARY.md`
</output>
