[project]
name = "research-tui"
version = "1.0.0"
description = "Terminal UI for Research Pilot backend"
requires-python = ">=3.10"
dependencies = [
    "textual>=0.83.0",
    "httpx>=0.27.0",
    "websockets>=13.0",
]

[project.scripts]
research = "research_tui.main:run"
