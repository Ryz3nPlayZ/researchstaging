---
phase: 14-production-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend3/package.json
  - frontend3/eslint.config.js
  - frontend3/components/*.tsx
  - frontend3/pages/*.tsx
  - frontend3/lib/*.tsx
autonomous: true
must_haves:
  truths:
    - "ESLint runs without configuration errors"
    - "All source files pass ESLint checks (0 warnings)"
    - "ESLint configuration follows React 19 and TypeScript best practices"
  artifacts:
    - path: "frontend3/eslint.config.js"
      provides: "ESLint configuration for React 19 + TypeScript"
    - path: "frontend3/package.json"
      contains: "eslint"
  key_links:
    - from: "package.json"
      to: "eslint"
      via: "devDependencies installation"
---

<objective>
Set up ESLint configuration and resolve all code quality warnings in frontend3.

Purpose: Establish code quality tooling and eliminate technical debt from frontend integration work.
Output: Working ESLint setup with zero warnings across all source files.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@frontend3/package.json
@frontend3/App.tsx
@frontend3/lib/api.ts
@frontend3/lib/context.tsx
@frontend3/lib/websocket.ts
@frontend3/lib/auth.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up ESLint with React 19 and TypeScript configuration</name>
  <files>frontend3/package.json, frontend3/eslint.config.js</files>
  <action>
    Install ESLint v9+ with required plugins:
    - `npm install --save-dev eslint @eslint/js typescript-eslint eslint-plugin-react eslint-plugin-react-hooks eslint-plugin-react-refresh`

    Create `eslint.config.js` with:
    - `@eslint/js` recommended configs
    - `typescript-eslint` parser and recommended configs
    - React plugin with JSX runtime settings
    - React Hooks plugin for hook rules
    - Ignore patterns for node_modules, dist, build

    Add `lint` script to package.json: `"lint": "eslint ."`
  </action>
  <verify>
    - `npx eslint frontend3/App.tsx` runs without configuration errors
    - `npm run lint` executes without "No ESLint configuration found" error
  </verify>
  <done>ESLint configured and runnable on the project</done>
</task>

<task type="auto">
  <name>Task 2: Scan all source files and identify ESLint warnings</name>
  <files>frontend3/App.tsx, frontend3/components/*.tsx, frontend3/pages/*.tsx, frontend3/lib/*.tsx</files>
  <action>
    Run ESLint on all TypeScript/React files:
    - `npx eslint App.tsx components/*.tsx pages/*.tsx lib/*.tsx --ext .ts,.tsx`

    Capture all warnings and errors. Common issues to look for:
    - Missing React imports (not needed in React 19, but may be flagged)
    - Unused variables
    - Missing dependencies in useEffect/useCallback
    - Type safety issues
    - Prop types validation issues
  </action>
  <verify>
    - ESLint output captured showing all current warnings
    - Document count of warnings by file and category
  </verify>
  <done>Complete list of ESLint warnings documented</done>
</task>

<task type="auto">
  <name>Task 3: Fix all identified ESLint warnings</name>
  <files>frontend3/App.tsx, frontend3/components/*.tsx, frontend3/pages/*.tsx, frontend3/lib/*.tsx, frontend3/eslint.config.js</files>
  <action>
    For each warning identified:
    1. Add ESLint disable comments ONLY for legitimate false positives (justify in comment)
    2. Fix actual issues:
       - Remove unused imports and variables
       - Fix missing dependencies in useEffect/useCallback
       - Resolve type safety issues
       - Add proper TypeScript types where missing
    3. For React 19 specific issues, update ESLint config to recognize React 19 patterns

    Update eslint.config.js if needed to:
    - Set react version to "19.0.0"
    - Adjust rules for React 19 compatibility
    - Enable/disable specific rules based on project needs

    Re-run `npm run lint` until 0 warnings achieved.
  </action>
  <verify>
    - `npm run lint` completes with exit code 0
    - No warnings or errors in ESLint output
  </verify>
  <done>Zero ESLint warnings across all source files</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ESLint runs successfully: `npm run lint`
- [ ] Zero warnings in ESLint output
- [ ] eslint.config.js exists with proper React 19 + TypeScript config
- [ ] package.json has "lint" script
- [ ] All source files checked: App.tsx, components/, pages/, lib/
</verification>

<success_criteria>

- ESLint configuration exists and runs without errors
- All source files pass ESLint checks with zero warnings
- package.json includes lint script
- Project is ready for manual testing phase
</success_criteria>

<output>
After completion, create `.planning/phases/14-production-polish/14-01-SUMMARY.md` documenting:
- ESLint configuration details (plugins, rules)
- Number and type of warnings found and fixed
- Any false positives with disable comments and justification
- Final lint output showing 0 warnings
</output>
