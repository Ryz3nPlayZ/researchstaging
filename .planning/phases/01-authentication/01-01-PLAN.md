---
phase: 01-authentication
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/server.py
  - frontend/src/App.js
  - frontend/src/context/AuthContext.js
  - frontend/src/lib/api.js
autonomous: false

must_haves:
  truths:
    - "User can sign in with Google OAuth"
    - "User account is automatically created on first login"
    - "User session persists across browser refresh"
    - "User can sign out from any page"
  artifacts:
    - path: "backend/server.py"
      provides: "Auth API endpoints (/api/auth/login, /api/auth/me, /api/auth/logout)"
      exports: ["POST /api/auth/login", "GET /api/auth/me", "POST /api/auth/logout"]
    - path: "frontend/src/context/AuthContext.js"
      provides: "Auth state management with localStorage persistence"
      min_lines: 50
    - path: "frontend/src/pages/LoginPage.js"
      provides: "Google OAuth login UI"
      min_lines: 80
  key_links:
    - from: "frontend/src/pages/LoginPage.js"
      to: "/api/auth/login"
      via: "fetch on OAuth callback"
      pattern: "fetch.*api/auth/login"
    - from: "frontend/src/lib/api.js"
      to: "localStorage"
      via: "setItem/getItem for token persistence"
    - from: "AuthContext"
      to: "all protected pages"
      via: "useAuth hook for auth state"
---

<objective>
Integrate Google OAuth authentication system by connecting existing auth_service to FastAPI routes, building frontend auth UI, and implementing session management.

Purpose: Enable secure user access via Google OAuth with persistent sessions across browser refresh.
Output: Working authentication flow with login, session persistence, and logout.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
@~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/STACK.md
@backend/auth_service.py
@backend/database/credit_models.py
@backend/server.py
@frontend/src/App.js
@frontend/src/lib/api.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Register auth API endpoints in FastAPI server</name>
  <files>backend/server.py</files>
  <action>
    Import auth_service from auth_service.py. Add three auth endpoints to api_router:

    1. POST /api/auth/login - Accepts {code: str}, calls auth_service.authenticate_user(code, db), returns {user, token}
    2. GET /api/auth/me - Accepts Authorization header, calls auth_service.get_current_user(token, db), returns user object
    3. POST /api/auth/logout - Returns success (client-side token deletion)

    Use get_db dependency injection for database sessions. Add proper error handling with HTTPException.

    IMPORTANT: auth_service already exists and implements OAuth flow, JWT generation, and user creation. Only routes needed.
  </action>
  <verify>
    - Run backend: python server.py
    - Check Swagger docs at http://localhost:8000/docs
    - Verify /api/auth/login, /api/auth/me, /api/auth/logout endpoints appear
  </verify>
  <done>Auth endpoints registered in FastAPI and appear in Swagger docs</done>
</task>

<task type="auto">
  <name>Task 2: Create AuthContext for frontend state management</name>
  <files>frontend/src/context/AuthContext.js</files>
  <action>
    Create React Context for authentication state:

    - State: user (object with id, email, name, picture_url, credits_remaining), token (string), loading (boolean)
    - Methods: login(code), logout(), checkAuth()
    - Persistence: Save token to localStorage on login, load on mount
    - API integration: Use fetch to call /api/auth/me endpoint with Authorization header
    - Provider component wraps children, exports useAuth hook

    Follow existing context pattern from ProjectContext.js (use createContext, useState, useEffect).

    Token persistence across refresh:
    - On login: localStorage.setItem('auth_token', token)
    - On mount: localStorage.getItem('auth_token') → checkAuth()
    - On logout: localStorage.removeItem('auth_token')
  </action>
  <verify>
    - Context file compiles without errors
    - Exports AuthContext and useAuth hook
    - Has localStorage integration for token
  </verify>
  <done>AuthContext created with user state, token persistence, and API integration</done>
</task>

<task type="auto">
  <name>Task 3: Create Google OAuth login page UI</name>
  <files>frontend/src/pages/LoginPage.js</files>
  <action>
    Create login page component with:

    - Google OAuth button: "Sign in with Google"
    - OAuth flow: Window popup to Google auth URL
    - Callback handling: Receive auth code, call login(code), close popup
    - Loading state during authentication
    - Error handling for failed auth

    OAuth flow implementation:
    1. Get Google auth URL from GET /api/auth/google-url (add this endpoint to server.py task)
    2. Open popup: window.open(authUrl, '_blank', 'width=500,height=600')
    3. Popup redirects to GOOGLE_REDIRECT_URI (http://localhost:5173/auth/callback?code=xxx)
    4. Main window receives code via message event or callback route
    5. Call login(code) → POST /api/auth/login
    6. On success: save token, redirect to /

    Use Shadcn UI button component if available, otherwise styled button.
  </action>
  <verify>
    - Component renders without errors
    - Contains "Sign in with Google" button
    - Has popup OAuth flow implementation
  </verify>
  <done>Login page with Google OAuth button and popup flow</done>
</task>

<task type="auto">
  <name>Task 4: Add protected route logic to App.js</name>
  <files>frontend/src/App.js</files>
  <action>
    Modify App.js to:

    - Wrap app with AuthContext.Provider
    - Add loading state: Show spinner while checking auth on mount
    - Route protection: Redirect to /login if not authenticated
    - Add /login route that renders LoginPage
    - Add /auth/callback route to handle OAuth return (receives code, calls login, redirects)
    - Show user info in header when authenticated
    - Add logout button that calls logout(), redirects to /login

    Existing routes (/, /projects/:id, etc.) remain protected.

    Auth check flow on mount:
    1. App mounts → AuthContext checks localStorage for token
    2. If token exists → call /api/auth/me to validate and get user
    3. If valid → set user state, show app
    4. If invalid or no token → redirect to /login
  </action>
  <verify>
    - npm start succeeds without errors
    - Visiting http://localhost:5173 redirects to /login when not authenticated
    - After login, redirects back to /
    - Refreshing page maintains login session
  </verify>
  <done>App protects routes and shows login page when unauthenticated</done>
</task>

<task type="auto">
  <name>Task 5: Add auth methods to frontend API client</name>
  <files>frontend/src/lib/api.js</files>
  <action>
    Add authentication methods to api.js:

    - login(code): POST to /api/auth/login, returns {user, token}
    - logout(): POST to /api/auth/logout, clears token
    - getMe(): GET to /api/auth/me with Authorization header, returns user
    - setAuthToken(token): Store token for all future requests
    - clearAuthToken(): Remove token

    Modify existing fetch calls to include Authorization header if token exists:
    - headers: { 'Authorization': `Bearer ${token}` }

    Token is stored in memory variable, synced from localStorage via AuthContext.
  </action>
  <verify>
    - api.js compiles without errors
    - login(), logout(), getMe() functions exported
    - Authorization header logic added to fetch wrapper
  </verify>
  <done>API client has auth methods and automatic token injection</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Google OAuth authentication flow with login, session persistence, and logout</what-built>
  <how-to-verify>
    1. Start backend: cd backend && python server.py (runs on :8000)
    2. Start frontend: cd frontend && yarn start (runs on :5173)
    3. Visit http://localhost:5173
    4. Verify: Redirects to /login page
    5. Click "Sign in with Google"
    6. Complete Google OAuth flow in popup
    7. Verify: Redirected back to app, user info visible
    8. Refresh page (F5)
    9. Verify: Still logged in, session persisted
    10. Click logout button
    11. Verify: Redirected to /login, session cleared
  </how-to-verify>
  <resume-signal>Type "approved" if authentication flow works end-to-end, or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three auth endpoints registered in FastAPI (login, me, logout)
- [ ] AuthContext manages user state with localStorage persistence
- [ ] Login page renders Google OAuth button and handles popup flow
- [ ] App.js protects routes and integrates AuthContext
- [ ] api.js has auth methods with Authorization header support
- [ ] User can sign in with Google OAuth
- [ ] User account auto-created on first login (verify via backend logs)
- [ ] Session persists across browser refresh
- [ ] User can sign out from any page
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- OAuth flow works end-to-end (Google popup → login → redirect)
- Session refresh persistence confirmed
- Logout clears session and redirects
- No console errors during auth flow
  </success_criteria>

<output>
After completion, create `.planning/phases/01-authentication/01-01-SUMMARY.md`
</output>
