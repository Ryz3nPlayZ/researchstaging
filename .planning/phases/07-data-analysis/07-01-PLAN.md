---
phase: 07-data-analysis
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/analysis_api.py
  - backend/agent_service.py
  - frontend/src/components/analysis/CodeEditor.jsx
  - frontend/src/lib/api.js
autonomous: true
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "User can request data analysis code generation from AI agent"
    - "AI agent generates Python code for data analysis"
    - "AI agent generates R code for data analysis"
    - "User can view generated code in code editor before execution"
    - "User can edit generated code before execution"
    - "Code editor supports syntax highlighting for Python and R"
  artifacts:
    - path: "backend/analysis_api.py"
      provides: "Code generation API endpoints"
      exports: ["/api/analysis/generate-code"]
      min_lines: 80
    - path: "backend/agent_service.py"
      provides: "AnalysisAgent for code generation"
      contains: "class AnalysisAgent"
    - path: "frontend/src/components/analysis/CodeEditor.jsx"
      provides: "Code editor component with syntax highlighting"
      min_lines: 100
  key_links:
    - from: "frontend/src/components/analysis/CodeEditor.jsx"
      to: "/api/analysis/generate-code"
      via: "POST request with analysis request"
      pattern: "fetch.*api/analysis/generate-code"
    - from: "backend/analysis_api.py"
      to: "backend/agent_service.py"
      via: "AnalysisAgent.generate_code() call"
      pattern: "AnalysisAgent.*generate"
    - from: "backend/agent_service.py"
      to: "backend/llm_service.py"
      via: "LLMService.generate_response() for code generation"
      pattern: "llm_service.generate"
---

<objective>
Implement AI-powered code generation for data analysis with user-editable code interface.

Purpose: Enable users to request Python or R code for data analysis tasks, view the generated code, and modify it before execution. Supports the full data analysis workflow from code generation through editing to execution.

Output: Code generation API, AnalysisAgent for code generation, and CodeEditor component with syntax highlighting.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Existing infrastructure:
# - backend/llm_service.py: Multi-provider LLM service with fallback support
# - backend/agent_service.py: AgentRouter and specialized agents (DocumentAgent, LiteratureAgent, MemoryAgent)
# - frontend/src/components/ai/AISidebar.jsx: Chat interface for AI interaction
# - frontend/src/context/ProjectContext.js: Global project state

# What needs to be built:
# 1. AnalysisAgent in agent_service.py for code generation
# 2. Code generation API endpoint (POST /api/analysis/generate-code)
# 3. CodeEditor component with Monaco Editor or CodeMirror for syntax highlighting
# 4. Integration with AI sidebar for code generation requests
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create AnalysisAgent and code generation API</name>
  <files>backend/analysis_api.py, backend/agent_service.py</files>
  <action>
Create AnalysisAgent in backend/agent_service.py:

1. Add AnalysisAgent class inheriting from Agent base class
2. Implement can_handle() to detect data analysis requests:
   - Keywords: "analyze", "statistic", "regression", "plot", "chart", "graph", "correlation"
   - Returns confidence score 0.8-1.0 for analysis queries
3. Implement handle() method for code generation:
   - Accepts parameters: task_description, language (python/r), data_context
   - Constructs prompt with code generation instructions
   - Calls LLM service with appropriate system prompt for code generation
   - Returns generated code with explanation

Create backend/analysis_api.py with FastAPI router:

1. POST /api/analysis/projects/{project_id}/generate-code
   - Accepts: {task: string, language: "python" | "r", data_context: dict}
   - Validates language parameter
   - Calls AnalysisAgent.generate_code()
   - Returns: {code: string, language: string, explanation: string}
   - Error handling for invalid language or LLM failures

Use existing LLM service patterns from agent_service.py.
</action>
  <verify>
grep -n "class AnalysisAgent" backend/agent_service.py
grep -n "generate-code" backend/analysis_api.py
</verify>
  <done>
AnalysisAgent class exists with can_handle() and handle() methods
Code generation API endpoint exists and accepts Python/R language parameter
</done>
</task>

<task type="auto">
  <name>Task 2: Create CodeEditor component with syntax highlighting</name>
  <files>frontend/src/components/analysis/CodeEditor.jsx, frontend/package.json</files>
  <action>
Create CodeEditor component at frontend/src/components/analysis/CodeEditor.jsx:

1. Install @monaco-editor/react for syntax highlighting:
   npm install @monaco-editor/react

2. Component props:
   - code: string (initial code content)
   - language: string ("python" or "r")
   - onChange: function (code, language) callback
   - onExecute: function (code, language) callback
   - readOnly: boolean (default false)

3. Component features:
   - Monaco Editor instance with Python/R syntax highlighting
   - Toolbar with: Run button, Copy button, Clear button
   - Line numbers and word wrap enabled
   - Responsive height (min 300px, max 70vh)
   - Dark theme matching app design

4. State management:
   - Local state for code content
   - Call onChange on code modification
   - Call onExecute when Run button clicked

Use Shadcn Button components for toolbar.
Follow existing component patterns from AIAssistant.jsx.
</action>
  <verify>
grep -n "MonacoEditor" frontend/src/components/analysis/CodeEditor.jsx
grep -n "onExecute" frontend/src/components/analysis/CodeEditor.jsx
</verify>
  <done>
CodeEditor component renders Monaco Editor with Python/R syntax highlighting
Run, Copy, Clear buttons implemented and functional
</done>
</task>

<task type="auto">
  <name>Task 3: Integrate code generation with AI sidebar and API client</name>
  <files>frontend/src/lib/api.js, frontend/src/components/ai/AISidebar.jsx</files>
  <action>
Update frontend/src/lib/api.js:

1. Add generateAnalysisCode() method:
   - POST /api/analysis/projects/{projectId}/generate-code
   - Accepts: {task, language, dataContext}
   - Returns: {code, language, explanation}
   - Error handling for invalid requests

Update frontend/src/components/ai/AISidebar.jsx:

1. Add "Generate Analysis Code" quick action button
2. When clicked, show dialog with:
   - Task description textarea
   - Language selector (Python/R dropdown)
   - Data context selector (optional file selection)
3. On submit, call generateAnalysisCode() API
4. Open CodeEditor modal/dialog with generated code
5. User can edit code before execution
6. Add "Execute" button to CodeEditor (connects to Plan 07-02 execution endpoint)

Use Shadcn Dialog component for code generation dialog.
Use existing toast patterns for success/error feedback.
Follow established patterns from plan proposal UI (Plan 06-03).
</action>
  <verify>
grep -n "generateAnalysisCode" frontend/src/lib/api.js
grep -n "Generate Analysis Code" frontend/src/components/ai/AISidebar.jsx
</verify>
  <done>
AI sidebar has "Generate Analysis Code" button
Code generation dialog opens with task, language, and context fields
Generated code appears in CodeEditor for user editing
</done>
</task>

</tasks>

<verification>
- [ ] npm install @monaco-editor/react succeeds
- [ ] AnalysisAgent.can_handle() detects analysis queries with confidence scoring
- [ ] POST /api/analysis/generate-code returns generated Python/R code
- [ ] CodeEditor component renders Monaco Editor with syntax highlighting
- [ ] AI sidebar "Generate Analysis Code" button opens dialog and generates code
- [ ] User can edit generated code in CodeEditor before execution
</verification>

<success_criteria>
- All tasks completed
- Code generation API returns valid Python or R code
- CodeEditor displays code with proper syntax highlighting
- User can modify generated code
- No errors or warnings introduced
</success_criteria>

<output>
After completion, create `.planning/phases/07-data-analysis/07-01-SUMMARY.md`
</output>
