---
phase: 22-fix-all-bugs
plan: 04
type: execute
wave: 4
depends_on: ["22-01", "22-02", "22-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "All 6 P0 blocker bugs verified fixed (literature search, analysis, chat, bibliography, download, navigation)"
    - "All 12 P1 major bugs verified fixed (dashboard UX, project creation, editor UX, analysis UX, settings, files, library)"
    - "All 5 P2 minor bugs verified fixed (branding, dead elements, padding, sidebar)"
    - "No regressions introduced - previously working features still work"
    - "All 10 core backend flows tested and passing"
    - "All 10 core user workflows tested and passing"
  artifacts:
    - path: ".planning/phases/22-fix-all-bugs/22-04-REGRESSION-RESULTS.md"
      provides: "Regression testing results document"
  key_links:
    - from: "22-04 regression testing"
      to: "Phase 20 test flows"
      via: "Re-running all test flows to verify no regressions"
      pattern: "test.*flow.*pass"
---

<objective>
Complete regression testing to verify all bug fixes don't break other functionality.

Purpose: Ensure that fixing bugs hasn't introduced new issues. Re-test all 20 test flows from Phase 20 to confirm everything still works. Document final results before moving to Phase 23 (Production Readiness).

Output: Comprehensive regression testing results with pass/fail status for all test flows, confirmation that all P0/P1/P2 bugs are fixed, and no new regressions introduced.
</objective>

<execution_context>
@/home/zemul/.claude/get-shit-done/workflows/execute-plan.md
@/home/zemul/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/20-manual-browser-testing/20-01-RESULTS.md
@.planning/phases/22-fix-all-bugs/22-01-SUMMARY.md
@.planning/phases/22-fix-all-bugs/22-02-SUMMARY.md
@.planning/phases/22-fix-all-bugs/22-03-SUMMARY.md
@.planning/ROADMAP.md

# Regression Testing Scope

After fixing all 22 bugs (6 P0, 12 P1, 5 P2), we must verify:

1. **All P0 bugs remain fixed** - Core functionality works
2. **All P1 bugs remain fixed** - UX improvements work
3. **All P2 bugs remain fixed** - Visual polish maintained
4. **No new regressions** - Previously working features still work
5. **All 10 backend flows pass** - API endpoints respond correctly
6. **All 10 user workflows pass** - End-to-end user journeys work

# Test Flows (from Phase 20)

## Backend API Flows (10)
1. Authentication: Mock login works, session persists
2. File Management: Upload, delete, organize files
3. Document CRUD: Create, edit, auto-save, delete
4. Literature Search: Semantic Scholar API returns papers
5. Citations: @-mention citations, bibliography generation
6. AI Chat: All 4 agent types respond correctly
7. Analysis Execution: Python/R code executes, results display
8. Document Export: PDF/DOCX export works
9. Memory/Graph: Claims storage, findings, relationships
10. WebSocket: Real-time updates, auto-save, connection status

## User Workflows (10)
1. Create new project
2. Upload files via drag-drop
3. Write and format text in TipTap editor
4. Insert citations and generate bibliography
5. Search literature and import papers
6. Execute data analysis and view results
7. Export document to PDF
8. Export document to DOCX
9. Chat with AI agent (all 4 types)
10. View memory/graph (claims, findings, relationships)

## Responsive Design (3 viewports)
1. Mobile (375px): Layout usable, hamburger menu, no horizontal scroll
2. Tablet (768px): Sidebar collapsible, AI panel works
3. Desktop (1280px+): Full layout with all panels
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>All 22 bug fixes from plans 22-01, 22-02, 22-03</what-built>
  <how-to-verify>
    1. Start backend: `cd backend && python server.py`
    2. Start frontend: `cd frontend3 && yarn start`
    3. Open browser to http://localhost:3000
    4. Test each P0 bug fix:
       - Literature Search: Go to Library view, search for "machine learning", should return papers
       - Analysis Execution: Go to Analysis view, run `print("hello")`, should show output
       - AI Chat: Go to Editor view, send message to AI, should get response
       - Bibliography: Create document, insert citation, generate bibliography
       - File Download: Upload file, click download button
       - Project Navigation: Click project tile, should navigate to project
    5. Test each P1 bug fix:
       - Dashboard filter: Click filter, select option, projects filter
       - Grid/List toggle: Click toggle, view switches
       - 3-dot menu: Click 3-dot on project, menu opens
       - Create project: Create project, auto-navigates to new project
       - "New project" tile: Appears first in list
       - Recent files: Upload file, shows in recent files
       - Library filters: Click filter buttons, active state shows
    6. Verify P2 fixes:
       - Single logo visible (check top bar and sidebar)
       - Dead elements grayed out (search, notifications, profile)
       - Editor toolbar padding consistent

    Type "approved" if all bug fixes verified working, or describe issues found.
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues found</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete application after all bug fixes</what-built>
  <how-to-verify>
    Run all 10 backend API flow tests:

    1. **Authentication**: Verify app loads without auth errors (mock login works)
    2. **File Management**: Upload PDF, verify it appears in file list, delete works
    3. **Document CRUD**: Create document, edit text, refresh page, verify persistence
    4. **Literature Search**: Search "climate change", verify papers return with data
    5. **Citations**: Insert citation in document, verify it appears
    6. **AI Chat**: Send message to each agent type (Document, Literature, Memory, General)
    7. **Analysis Execution**: Run Python code `print("test")`, verify output shows
    8. **Document Export**: Export document to PDF and DOCX
    9. **Memory/Graph**: View Memory page, verify claims/findings display
    10. **WebSocket**: Check editor toolbar shows connection status

    For each test:
    - If PASS: Note "PASS - [brief description]"
    - If FAIL: Note "FAIL - [error description]"

    Type results in format: "1:PASS, 2:PASS, 3:FAIL - [reason], ..."
  </how-to-verify>
  <resume-signal>Type test results: "1:PASS, 2:PASS, ..." or describe failures</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete application after all bug fixes</what-built>
  <how-to-verify>
    Run all 10 user workflow tests end-to-end:

    1. **Create Project**: Click "Create Project", verify project created and appears in list
    2. **Upload Files**: Drag-drop a file, verify upload progress and success
    3. **Write Text**: In editor, type text and apply formatting (bold, italic, heading)
    4. **Insert Citations**: Click "Insert Citation", search, select paper, verify citation appears
    5. **Search Literature**: Search papers, click on result to view details
    6. **Execute Analysis**: Write code, click Execute, verify results display
    7. **Export PDF**: Create document, export to PDF, verify file downloads
    8. **Export DOCX**: Create document, export to DOCX, verify file downloads
    9. **AI Chat**: Open chat panel, send message, verify AI response
    10. **View Memory**: Navigate to Memory view, verify claims/findings display

    For each test:
    - If PASS: Note "PASS - [brief description]"
    - If FAIL: Note "FAIL - [error description]"

    Type results in format: "1:PASS, 2:PASS, 3:FAIL - [reason], ..."
  </how-to-verify>
  <resume-signal>Type test results: "1:PASS, 2:PASS, ..." or describe failures</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete application after all bug fixes</what-built>
  <how-to-verify>
    Test responsive design at 3 viewports:

    1. **Desktop (1280px+)**: Open browser at full width
       - Verify: Full sidebar + main content + AI chat panel visible
       - Verify: No horizontal scroll
       - Verify: All navigation labels visible

    2. **Tablet (768px)**: Resize browser to 768px width
       - Verify: Content still readable
       - Verify: No horizontal scroll
       - Verify: Layout adapts (panels stack or hide)

    3. **Mobile (375px)**: Resize browser to 375px width
       - Verify: Hamburger menu appears
       - Verify: Click hamburger opens sidebar drawer
       - Verify: No horizontal scroll

    For each viewport:
    - If PASS: Note "PASS - [brief description]"
    - If FAIL: Note "FAIL - [issue description]"

    Type results in format: "Desktop:PASS, Tablet:PASS, Mobile:FAIL - [reason]"
  </how-to-verify>
  <resume-signal>Type responsive test results or describe issues</resume-signal>
</task>

<task type="auto">
  <name>Document regression testing results</name>
  <files>.planning/phases/22-fix-all-bugs/22-04-REGRESSION-RESULTS.md</files>
  <action>
    Create a comprehensive regression testing results document.

    Include:
    1. Summary of all bug fixes (22-01, 22-02, 22-03)
    2. Backend API flow test results (10 tests)
    3. User workflow test results (10 tests)
    4. Responsive design test results (3 viewports)
    5. Overall pass rate
    6. Any issues found
    7. Recommendations for Phase 23 (Production Readiness)

    Format:
    ```markdown
    # Phase 22-04: Regression Testing Results

    ## Bug Fixes Summary
    - P0 Blockers Fixed: 6/6
    - P1 Major Fixed: 12/12
    - P2 Minor Fixed: 5/5

    ## Backend API Flow Results
    | # | Flow | Status | Notes |
    |---|------|--------|-------|
    | 1 | Authentication | PASS/FAIL | ... |
    ...

    ## User Workflow Results
    | # | Flow | Status | Notes |
    |---|------|--------|-------|
    ...

    ## Responsive Design Results
    | Viewport | Status | Issues |
    |----------|--------|--------|
    ...

    ## Overall Results
    - Total Tests: 23
    - Passed: X
    - Failed: Y
    - Pass Rate: Z%

    ## Issues Found
    (List any new issues or remaining bugs)

    ## Recommendation
    (Ready for Phase 23 or needs more work)
    ```

    Use the checkpoint results to populate this document.
  </action>
  <verify>22-04-REGRESSION-RESULTS.md created with all test results documented</verify>
  <done>Regression testing results documented with pass/fail status for all tests</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. All 22 bugs verified fixed (P0: 6/6, P1: 12/12, P2: 5/5)
2. All 10 backend flows tested and documented
3. All 10 user workflows tested and documented
4. Responsive design verified at 3 viewports
5. Regression testing results document created

If any tests fail, document issues in results file. The failures will need to be addressed before Phase 24 (Ship Decision).
</verification>

<success_criteria>
1. All P0 bugs remain fixed after testing
2. All P1 bugs remain fixed after testing
3. All P2 bugs remain fixed after testing
4. Backend API test results: >= 90% pass rate
5. User workflow test results: >= 90% pass rate
6. Responsive design: All 3 viewports pass
7. Regression results document created
</success_criteria>

<output>
After completion, create `.planning/phases/22-fix-all-bugs/22-04-SUMMARY.md` and `.planning/phases/22-fix-all-bugs/22-04-REGRESSION-RESULTS.md` documenting:
- All bug fixes verified working
- Test results for all flows
- Any remaining issues
- Readiness assessment for Phase 23 (Production Readiness)
</output>
