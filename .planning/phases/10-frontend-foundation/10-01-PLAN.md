---
phase: 10-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [frontend3/package.json, frontend3/vite.config.ts]
autonomous: true
user_setup: []

# Goal-backward verification (derived during planning, verified after execution)
must_haves:
  truths:
    - "Development server starts on port 3000 without errors"
    - "Vite build completes successfully with TypeScript compilation"
    - "No TypeScript compilation errors in console"
    - "Frontend displays UI with Dashboard, Files, Library, Editor views"
  artifacts:
    - path: "frontend3/package.json"
      provides: "Project dependencies and scripts"
      contains: "\"scripts\": {\"dev\": \"vite\", \"build\": \"vite build\"}"
    - path: "frontend3/vite.config.ts"
      provides: "Vite build configuration"
      contains: "export default defineConfig"
  key_links:
    - from: "frontend3/package.json"
      to: "node_modules installation"
      via: "npm install command"
      pattern: "npm install"
---

<objective>
Initialize frontend3 project and verify build system works.

Purpose: Establish that the frontend3 project structure is valid and can build successfully. This plan verifies the existing setup from researchai-workspace.zip works in our environment.

Output: Working development server on port 3000 with successful Vite build.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

[Existing frontend structure:]
@frontend3/package.json
@frontend3/vite.config.ts
@frontend3/tsconfig.json
@frontend3/index.tsx
@frontend3/App.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install frontend3 dependencies</name>
  <files>frontend3/package.json</files>
  <action>
    Navigate to frontend3 directory and install dependencies using npm:
    ```bash
    cd frontend3 && npm install
    ```

    This will install:
    - react@^19.2.4
    - react-dom@^19.2.4
    - @google/genai@^1.40.0 (will be removed in later phases)
    - vite@^6.2.0
    - typescript@~5.8.2
    - @vitejs/plugin-react@^5.0.0

    Do NOT modify package.json. The existing dependencies from researchai-workspace.zip are correct.
  </action>
  <verify>
    Run: ls frontend3/node_modules | head -10
    Expected: node_modules directory created with packages installed
  </verify>
  <done>
    Dependencies installed successfully, node_modules directory exists
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify Vite development server starts</name>
  <files>frontend3/vite.config.ts</files>
  <action>
    Start the Vite development server:
    ```bash
    cd frontend3 && npm run dev
    ```

    Let it run for 5 seconds to verify startup, then check console output for:
    - "Local: http://localhost:3000/" message
    - No TypeScript errors
    - No build failures

    Expected output pattern:
    - VITE v6.x.x ready in xxx ms
    - ➜ Local: http://localhost:3000/

    Press Ctrl+C to stop the server after verification.

    Do NOT modify vite.config.ts. The existing configuration (port 3000, React plugin, path alias @/*) is correct.
  </action>
  <verify>
    Run: cd frontend3 && timeout 10 npm run dev 2>&1 | grep -E "(Local:|ready|error)"
    Expected: "Local: http://localhost:3000/" appears, no errors
  </verify>
  <done>
    Development server starts successfully on port 3000 without errors
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify Vite production build</name>
  <files>frontend3/vite.config.ts, frontend3/tsconfig.json</files>
  <action>
    Run Vite production build to verify TypeScript compilation:
    ```bash
    cd frontend3 && npm run build
    ```

    Check output for:
    - "building for production..." message
    - dist/ directory created
    - No TypeScript errors
    - Build completes successfully

    Expected artifacts in dist/:
    - index.html
    - assets/*.js files
    - assets/*.css files

    Do NOT modify build configuration. The existing Vite + TypeScript setup is correct.
  </action>
  <verify>
    Run: ls -la frontend3/dist/
    Expected: dist directory exists with index.html and assets/ subdirectory
  </verify>
  <done>
    Production build completes successfully, dist/ directory created with all assets
  </done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] npm install completed without errors
- [ ] npm run dev starts server on port 3000
- [ ] npm run build completes without TypeScript errors
- [ ] dist/ directory created with production assets
</verification>

<success_criteria>

- All tasks completed
- Development server runs on port 3000
- Production build succeeds with TypeScript compilation
- No errors or warnings in console
- Frontend3 project structure verified working
  </success_criteria>

<output>
After completion, create `.planning/phases/10-frontend-foundation/10-01-SUMMARY.md`
</output>
